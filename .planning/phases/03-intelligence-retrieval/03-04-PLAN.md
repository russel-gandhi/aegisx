---
phase: 03-intelligence-retrieval
plan: 04
type: execute
wave: 3
depends_on: ["03-02"]
files_modified:
  - infra/postgres/seed/002_urs_fixture.sql
  - infra/apply-seed.sh
  - infra/verify-seed.sh
  - backend/app/agents/a2_compliance.py
  - backend/tests/test_a2_compliance.py
autonomous: true
requirements: [ORC-03]

estimate:
  tokens: 84000
  raw_tokens: 84000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "All three Bible-named A2 checks — `verify_urs_approved`, `verify_periodic_eval_current`, `verify_test_traceability` — run as parameterized queries against live Postgres and each has a passing test for both its pass and its fail path (ORC-03, D-03)."
    - "`verify_urs_approved`'s positive path is exercised against a real APPROVED URS row, not asserted by inspection, because an additive idempotent seed fixture supplies one (D-05)."
    - "A2 emits one `AgentFinding` per failed check and none for a passing check; the LLM narration can never change which checks passed (Bible Section 1.3, D-03)."
    - "`bash infra/apply-seed.sh` applies every script in `infra/postgres/seed/` in sorted order and stays safely re-runnable."
  artifacts:
    - infra/postgres/seed/002_urs_fixture.sql
    - backend/tests/test_a2_compliance.py
  key_links:
    - "`infra/apply-seed.sh` -> every file in `infra/postgres/seed/`. It currently names `001_seed.sql` literally; a second seed file that the script never applies would make the URS positive-path test pass locally and fail in CI."
    - "each check's `rule_id` -> the OPA rule C1 uses to corroborate it. `ANNEX11-S4-DOC-001` is scoped to O&M documents in `policies/gxp_rules.rego`, so a URS-approval finding is deliberately NOT corroborated by the current bundle — that asymmetry must be documented, not silently patched by editing the closed policy bundle."
    - "the seeded `URS-042` -> `TC-2026-042` DRAFT link -> rule 5's positive fixture. The traceability check must mirror rule 5's DRAFT condition exactly so C1 corroborates the finding against real OPA output."
---

<objective>
Expand the 03-02 tracer along the compliance axis: bring A2 from one deterministic check to the full three the Bible names, and close the one testability gap 03-RESEARCH.md flagged by seeding a real APPROVED URS document so `verify_urs_approved`'s positive path is exercised rather than assumed (D-05).

Purpose: ORC-03 names all three functions explicitly. Two of them are unimplemented after the tracer, and one of those has no seeded row to test against.

Output: an additive, idempotent `infra/postgres/seed/002_urs_fixture.sql`; a glob-driven `infra/apply-seed.sh`; the two remaining check functions plus multi-gap finding assembly in `backend/app/agents/a2_compliance.py`; and `backend/tests/test_a2_compliance.py`.

Scope boundary: the schema is closed for this phase (CLAUDE.md Rule 7; 03-RESEARCH.md Pitfall 5). No new table or column may be invented to satisfy a check, and `policies/gxp_rules.rego` is Phase-2 territory and must not be edited — where policy coverage does not reach a check, that is recorded as a documented limitation, not closed by adding a rule.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-intelligence-retrieval/03-CONTEXT.md
@.planning/phases/03-intelligence-retrieval/03-RESEARCH.md
@.planning/phases/03-intelligence-retrieval/03-02-SUMMARY.md
@backend/app/agents/a2_compliance.py
@backend/README.md
@infra/postgres/seed/001_seed.sql
@infra/apply-seed.sh
@infra/verify-seed.sh
@policies/gxp_rules.rego
</context>

<critical_findings>
**Rule 10 file ownership.** This plan runs in parallel with `03-03`. It owns `backend/app/agents/a2_compliance.py`, the seed and infra scripts, and its own test module. It must not touch `backend/app/graph/state.py`, `backend/app/agents/c1_verifier.py`, `backend/app/agents/a0_orchestrator.py`, or `backend/app/agents/minimal_specialists.py` — all of those are `03-03`'s or a later plan's. The A2 graph node already delegates to `run_a2` from wave 2, so nothing in `state.py` needs to change for A2 to grow.

**Policy coverage is asymmetric across the three checks, and that is correct.** `ANNEX11-S11-PE-001` (rule 7) and `ANNEX11-S4-TRC-001` (rule 5) both fire on the seeded records, so C1 corroborates those findings and scores them `MEDIUM`. `ANNEX11-S4-DOC-001` (rule 1) is scoped to `doc_type == "O&M"` only, so a URS-approval gap is not corroborated and C1 correctly returns `INSUFFICIENT_EVIDENCE` for it. That is the deterministic-first thesis working, not a defect — an agent claim the formal policy cannot corroborate is not trusted. Record it as a limitation; do not edit the closed Rego bundle to make it symmetric.

**The seed fixture is a Phase-3 test fixture, not Bible Section 5 data.** `001_seed.sql`'s header states that every literal in it is byte-identical to the Bible and that no additional rows were invented for any other table. The new file must say plainly, in its own header, that it is additive Phase-3 fixture data authorized by D-05, that it exists to give `verify_urs_approved` a positive path, and that it is routed to SENT-7-05 for reconciliation. Do not edit `001_seed.sql`.
</critical_findings>

<tasks>

<task type="auto">
  <name>Task 1: Additive URS seed fixture (D-05) and a glob-driven apply-seed</name>
  <files>infra/postgres/seed/002_urs_fixture.sql, infra/apply-seed.sh, infra/verify-seed.sh</files>
  <read_first>
    - infra/postgres/seed/001_seed.sql (the full header rationale, and the `INSERT ... ON CONFLICT (id) DO NOTHING` idempotency style every statement uses — match it exactly)
    - infra/apply-seed.sh (the schema-presence precondition, the `dc()` compose shim, the `ON_ERROR_STOP=1` invocation, and the `SEED FAILED` / `SEED APPLIED` output contract)
    - infra/verify-seed.sh (the `check_eq` helper and the one-line-per-check PASS/FAIL output contract)
    - infra/postgres/initdb/001_schema.sql lines 35-45 (the `documents` table's nine columns and their types)
    - infra/README.md line 135 onward (it documents `001_seed.sql` by name; update the wording if it now misleads)
    - .planning/phases/03-intelligence-retrieval/03-CONTEXT.md ("Resolved: RESEARCH.md Open Questions", Open Question 1)
  </read_first>
  <action>
Create `infra/postgres/seed/002_urs_fixture.sql` containing exactly one statement: an insert into `documents` of id `DOC-2026-URS-01`, system id `GXP-MFG-DEMO-01`, doc type `URS`, title `NovaSynth User Requirements Specification`, version `v1.0`, author `Sarah Jensen`, a created date of `2026-07-01 09:00:00`, an effective date of `2026-07-15 00:00:00`, and status `APPROVED`, terminated with the same `ON CONFLICT (id) DO NOTHING` guard every statement in `001_seed.sql` uses. Every column named in `infra/postgres/initdb/001_schema.sql`'s `documents` DDL is supplied; no column outside it is referenced.

The file header must state: that this is Phase-3 additive fixture data, not Bible Section 5 seed data; that it exists so `verify_urs_approved`'s positive path is exercised against a real row rather than assumed (D-05, resolving 03-RESEARCH.md Open Question 1); that it adds no gap and removes none, because Rego rule 1 is scoped to `doc_type = 'O&M'` and therefore does not fire on this row, leaving the ten injected gaps and their violation count unchanged; and that it is routed to SENT-7-05 for Bible reconciliation.

Then modify `infra/apply-seed.sh` so the final psql invocation iterates every `.sql` file in `infra/postgres/seed/` in sorted filename order instead of naming one file. Preserve verbatim: the `.env` sourcing block, the schema-presence precondition and its error text, the `dc()` shim, `PGPASSWORD` passing, the `ON_ERROR_STOP=1` flag, the non-zero-status `SEED FAILED` branch, and the final `SEED APPLIED` line. Fail on the first script that errors rather than continuing. Update the header comment so it no longer names a single file, and keep the re-runnability note accurate. Echo each applied filename so an operator can see both scripts ran.

Then add two checks to `infra/verify-seed.sh` using its existing `check_eq` helper, placed after the ten gap checks under a new comment heading marking them as Phase-3 fixture assertions: that `DOC-2026-URS-01`'s `doc_type` is `URS`, and that its `status` is `APPROVED`. Change nothing else in that file — every existing check and the `SEED OK` / `SEED FAILED` contract stay as they are.

If `infra/README.md` describes the seed application in terms of a single named file, update only that wording to reflect the directory-wide behavior.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" &amp;&amp; bash infra/apply-seed.sh &amp;&amp; bash infra/apply-seed.sh</automated>
    <automated>cd "$(git rev-parse --show-toplevel)" &amp;&amp; bash infra/verify-seed.sh</automated>
    <automated>cd "$(git rev-parse --show-toplevel)" &amp;&amp; docker compose exec -T postgres psql -U sentinel -d sentinel -tA -c "SELECT count(*) FROM documents WHERE system_id='GXP-MFG-DEMO-01'"</automated>
  </verify>
  <acceptance_criteria>
    - `bash infra/apply-seed.sh` run twice in a row both times ends with `SEED APPLIED` and exits 0 — the fixture is idempotent.
    - `bash infra/verify-seed.sh` ends with `SEED OK`, exits 0, and its output contains zero `FAIL` lines.
    - The document count query returns `2` (the pre-existing O&M draft plus the new URS fixture).
    - `docker compose exec -T postgres psql -U sentinel -d sentinel -tA -c "SELECT status FROM documents WHERE id='DOC-2026-URS-01'"` returns `APPROVED`.
    - `grep -c 'ON CONFLICT (id) DO NOTHING' infra/postgres/seed/002_urs_fixture.sql` outputs `1`.
    - `grep -c 'SENT-7-05' infra/postgres/seed/002_urs_fixture.sql` outputs at least `1`.
    - `git diff --stat -- infra/postgres/seed/001_seed.sql` produces empty output — the Bible-literal seed was not edited.
    - `grep -c 'SEED APPLIED' infra/apply-seed.sh` outputs `1` and `grep -c 'ON_ERROR_STOP=1' infra/apply-seed.sh` outputs `1`.
    - `bash infra/verify-schema.sh` still exits 0.
  </acceptance_criteria>
  <done>A real APPROVED URS row exists for `GXP-MFG-DEMO-01`, `apply-seed.sh` applies every seed script in sorted order and is still safely re-runnable, and `verify-seed.sh` asserts the new fixture alongside the original ten gaps.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: A2's remaining two deterministic checks and multi-gap finding assembly</name>
  <files>backend/app/agents/a2_compliance.py, backend/tests/test_a2_compliance.py</files>
  <read_first>
    - backend/app/agents/a2_compliance.py (all of it — `verify_periodic_eval_current`, `narrate_gap`, `build_finding`, `run_a2`; the two new checks mirror the first one's return shape exactly)
    - backend/README.md section "AgentFinding conventions (Phase 3)" (the finding shape, the `UNVERIFIED` convention, and the rule-id-as-citation rule)
    - policies/gxp_rules.rego lines 27-39 (rule 1, `ANNEX11-S4-DOC-001`, scoped to `doc_type == "O&M"`) and lines 81-95 (rule 5, `ANNEX11-S4-TRC-001`, and its DRAFT condition)
    - infra/postgres/initdb/001_schema.sql lines 35-45 (`documents`), 54-59 (`requirements`), 77-81 (`test_cases`)
    - infra/postgres/seed/001_seed.sql (the `URS-042` requirement, its `TC-2026-042` DRAFT test case, and the absence of any URS document before this plan's Task 1)
    - GxP-Sentinel-Project-Bible-v6.md Section 2, the "A2 — Compliance & Audit Readiness Agent" entry (Role, the three deterministic check names, and the failure behavior text)
    - .planning/phases/03-intelligence-retrieval/03-RESEARCH.md (Pitfall 5 — the query shapes each check must be grounded in, and the prohibition on inventing a fourth table)
    - backend/tests/test_hero_tracer.py (the respx and `asyncio.run()` conventions, and the finding assertions it already makes)
  </read_first>
  <behavior>
    - `verify_urs_approved` against the seeded database returns a passing result whose record id is `DOC-2026-URS-01`.
    - `verify_urs_approved` for `BUS-IT-DEMO-02`, which has no URS document, returns a failing result with a null record.
    - `verify_urs_approved` returns a failing result when the only URS document's status is not APPROVED, exercised against a row inserted and rolled back inside a transaction.
    - `verify_test_traceability` against the seeded database returns a failing result naming requirement `URS-042`, whose linked test case `TC-2026-042` is DRAFT — the same condition Rego rule 5 fires on.
    - `verify_test_traceability` returns a passing result when every requirement for a system resolves to a non-DRAFT test case, exercised against rows inserted and rolled back inside a transaction.
    - `run_a2` for `GXP-MFG-DEMO-01` against the seeded database returns exactly two findings — the periodic-evaluation gap and the traceability gap — and none for the now-passing URS check.
    - Every finding `run_a2` emits conforms to the phase-3 conventions: `confidence_score` of `UNVERIFIED`, `regulatory_citations` holding exactly the check's rule id, `evidence_ids` holding the real record's primary key, all nine ALCOA fields present, and a non-empty `model_attribution`.
    - A finding produced for a check with no record carries an empty `evidence_ids` list and a `finding_id` that marks the missing record rather than fabricating one.
    - With a mocked provider response, each finding's `claim` equals the returned candidate text and its `model_attribution` is the Gemini model id; with no key present, each `claim` is the deterministic template and the attribution records the fallback — and in both runs the same two checks fail, proving narration cannot alter a result.
    - With Postgres unreachable, `run_a2` returns the Bible's A2 failure behavior — a `LOW`-confidence finding citing that traceability verification failed — and raises nothing.
  </behavior>
  <action>
Extend `backend/app/agents/a2_compliance.py` with the two remaining Bible-named checks, each returning the same result shape the tracer's check already returns (`check`, `rule_id`, `passed`, `record`):

  - `async def verify_urs_approved(pool, system_id: str) -> dict` — `SELECT id, status FROM documents WHERE system_id = $1 AND doc_type = 'URS' ORDER BY id LIMIT 1`, with `$1` binding the system id. `passed` is True only when a row exists and its status is `APPROVED`. Rule id `ANNEX11-S4-DOC-001`. This mirrors rule 1's approval-state pattern generalized from O&M to URS (03-RESEARCH.md Pitfall 5); see the coverage note below.
  - `async def verify_test_traceability(pool, system_id: str) -> dict` — selects each requirement for the system left-joined to its test case on `requirements.test_case_id = test_cases.id`, with `$1` binding the system id, and returns the first requirement whose linked test case is DRAFT or unresolvable. The DRAFT condition mirrors Rego rule 5 exactly. The unresolvable-link condition is a case rule 5 cannot express — a missing key makes its body undefined, so no violation is emitted — which means C1 will not corroborate such a finding. Note that asymmetry in the function docstring; do not close it by editing the policy bundle.

Add a module-level `A2_CHECKS` tuple naming the three checks in the Bible's own order so `run_a2` iterates data rather than repeating three call sites, and rewrite `run_a2` to run all three, narrate each failure, and return `{"findings": [...]}` with one finding per failed check and an empty list when all three pass. Keep the existing pool-unavailable branch returning the Bible's A2 failure behavior unchanged.

Extend `build_finding` so a check with a null record yields an empty `evidence_ids` list and a `finding_id` whose record segment marks the record as absent rather than inventing an identifier. Everything else about the finding shape stays exactly as `backend/README.md`'s conventions section fixes it.

Add a docstring block to the module recording the policy-coverage asymmetry: `ANNEX11-S11-PE-001` and `ANNEX11-S4-TRC-001` are corroborated by the live rule bundle and therefore score `MEDIUM`, while `ANNEX11-S4-DOC-001` is O&M-scoped and therefore leaves a URS-approval finding uncorroborated at `INSUFFICIENT_EVIDENCE`. State that this is the intended deterministic-first outcome, that the policy bundle is Phase-2 territory and closed here, and route the observation to SENT-7-05.

The narration path is unchanged and stays subordinate: every `passed` boolean is computed before any model call and no model output may alter one.

Write `backend/tests/test_a2_compliance.py` covering all ten behaviors against the live seeded Postgres, using `@respx.mock`, `monkeypatch` for key presence and for pointing the database URL at a closed port, and `asyncio.run()` inside plain `def test_*` functions. Tests that need rows the seed does not provide must insert them inside a transaction that is rolled back, so the suite leaves the seeded database byte-identical and stays re-runnable.
  </action>
  <verify>
    <automated>cd backend &amp;&amp; .venv/Scripts/python -m pytest tests/test_a2_compliance.py -x -q</automated>
    <automated>cd backend &amp;&amp; .venv/Scripts/python -c "from app.agents.a2_compliance import A2_CHECKS, verify_urs_approved, verify_periodic_eval_current, verify_test_traceability; assert len(A2_CHECKS)==3; print('ok')"</automated>
    <automated>cd backend &amp;&amp; .venv/Scripts/python -m pytest tests/test_hero_tracer.py -x -q</automated>
    <automated>cd "$(git rev-parse --show-toplevel)" &amp;&amp; bash infra/verify-seed.sh</automated>
    <automated>cd backend &amp;&amp; git diff --stat -- app/graph/state.py app/agents/c1_verifier.py</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_a2_compliance.py -x -q` exits 0 with at least 10 passing tests, one per listed behavior.
    - The one-liner prints `ok`, proving all three Bible-named check functions are importable and registered.
    - `pytest tests/test_hero_tracer.py -x -q` still exits 0 — wave 2's slice survives A2 growing from one check to three. If the tracer asserted exactly one finding, update that assertion to the two findings the seeded state now produces, and record the change in the summary.
    - `bash infra/verify-seed.sh` still ends with `SEED OK` after the full test run — every test-inserted row was rolled back.
    - `git diff --stat -- app/graph/state.py app/agents/c1_verifier.py` produces empty output — this plan did not cross the Rule 10 file boundary into `03-03`'s or a later plan's files.
    - `git diff --stat -- policies/` produces empty output — the closed Phase-2 policy bundle was not edited.
    - Every SQL statement in `backend/app/agents/a2_compliance.py` binds its identifier through a placeholder: `grep -v '^\s*#' backend/app/agents/a2_compliance.py | grep -cE "f\"SELECT|f'SELECT"` outputs `0`.
    - `grep -c 'SENT-7-05' backend/app/agents/a2_compliance.py` outputs at least `1`.
  </acceptance_criteria>
  <done>All three Bible-named A2 checks run as parameterized queries against live Postgres with both paths tested, `run_a2` emits exactly one conventions-compliant finding per failed check, and the seeded database is unchanged after the suite runs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user-supplied `system_id` -> three A2 SQL statements | An identifier that ultimately originates from user-facing input reaches three new queries |
| Postgres free-text columns -> narration prompt | `req_text`, `title`, and `description` values reach a model prompt |
| seed script -> live demo database | A committed SQL file mutates the state every demo and every test reads |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-18 | Tampering (SQL injection) | `verify_urs_approved`, `verify_test_traceability` | high | mitigate | asyncpg `$1` placeholders exclusively; gated by the `f"SELECT` grep check outputting `0`. |
| T-03-19 | Elevation of Privilege (indirect prompt injection) | narration over `req_text` and document titles | medium | mitigate | DB-sourced text enters the prompt as content to summarize; every `passed` boolean is computed before the call and cannot be altered by the response. C2's deterministic detector lands in Phase 5. |
| T-03-20 | Tampering (test pollution of demo state) | `test_a2_compliance.py` | medium | mitigate | Rows the seed does not provide are inserted inside a rolled-back transaction; `infra/verify-seed.sh` is re-run after the suite as a gate. |
| T-03-21 | Spoofing (uncorroborated claim scored as trusted) | the URS-approval finding | high | accept | Rule 1 is O&M-scoped, so a URS-approval gap is not corroborated and C1 returns `INSUFFICIENT_EVIDENCE`. Accepted deliberately: this is the deterministic-first design outcome, and closing it would require editing the closed Phase-2 policy bundle. Documented in the module and routed to SENT-7-05. |
| T-03-22 | Denial of Service | seed script applied repeatedly | low | mitigate | Every statement carries the conflict-noop guard, so repeated application is a no-op; asserted by running `apply-seed.sh` twice. |
</threat_model>

<verification>
With Postgres, Qdrant, and OPA up:

```bash
bash infra/health-check.sh
bash infra/apply-seed.sh
bash infra/verify-schema.sh
bash infra/verify-seed.sh
cd backend && .venv/Scripts/python -m pytest -q
```

Every gate must pass, and `verify-seed.sh` must still report `SEED OK` after the pytest run — that is the assertion that the test suite left the demo database untouched.

Wire contract vs. live quality (03-RESEARCH.md Pitfall 6): the mocked provider response proves A2's narration request and parsing. Whether a live Gemini narration reads well remains the operator follow-up recorded in plan 03-01's `user_setup`.
</verification>

<success_criteria>
- All three Bible-named A2 checks exist, are parameterized, and each has a passing and a failing test.
- `verify_urs_approved`'s positive path runs against a real seeded APPROVED URS row.
- `run_a2` on `GXP-MFG-DEMO-01` emits exactly two findings against the seeded state, both conventions-compliant.
- `apply-seed.sh` applies both seed scripts in sorted order and is idempotent; `verify-seed.sh` asserts the new fixture.
- No file owned by `03-03`, by a later plan, or by the closed Phase-2 policy bundle was modified.
</success_criteria>

## Artifacts this phase produces

Produced by **this plan** (03-04):

| Artifact | Symbols |
|----------|---------|
| `infra/postgres/seed/002_urs_fixture.sql` | `DOC-2026-URS-01` |
| `infra/apply-seed.sh` (modified) | sorted directory-wide application |
| `infra/verify-seed.sh` (modified) | two Phase-3 fixture checks |
| `backend/app/agents/a2_compliance.py` (extended) | `verify_urs_approved()`, `verify_test_traceability()`, `A2_CHECKS`, extended `build_finding()` and `run_a2()` |
| `backend/tests/test_a2_compliance.py` | 10+ tests |

Prior plans produced `db.py`, `llm_router.py`, the tracer's `a2_compliance.py` / `c1_verifier.py`, and the graph delegation. Wave sibling `03-03` produces `a0_orchestrator.py` and `minimal_specialists.py`. Later plans add the C1 Critical-review suite and the full hero-loop integration test.

<output>
Create `.planning/phases/03-intelligence-retrieval/03-04-SUMMARY.md` when done.
</output>
