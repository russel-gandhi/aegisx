---
phase: 03-intelligence-retrieval
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/requirements.txt
  - .env.example
  - backend/app/db.py
  - backend/app/llm_router.py
  - backend/app/schemas.py
  - backend/tests/conftest.py
  - backend/tests/test_db.py
  - backend/tests/test_llm_router.py
  - backend/README.md
autonomous: false
requirements: [ORC-02, ORC-03, EVID-01]
user_setup:
  - service: llm-providers
    why: "Live LLM classification/synthesis quality (A0 intent routing, A2 narration) cannot be exercised without provider keys. Wire-contract correctness IS proven this phase via respx-mocked responses; only live-quality verification is deferred. Per D-01 this is expected, not a blocker."
    env_vars:
      - name: GEMINI_API_KEY
        source: "Google AI Studio -> Get API key (aistudio.google.com/apikey)"
      - name: DEEPSEEK_API_KEY
        source: "DeepSeek Platform -> API keys (platform.deepseek.com/api_keys)"
      - name: GROQ_API_KEY
        source: "Groq Console -> API Keys (console.groq.com/keys)"
      - name: OPENROUTER_API_KEY
        source: "OpenRouter -> Keys (openrouter.ai/keys)"

estimate:
  tokens: 78000
  raw_tokens: 78000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A backend test can open a real asyncpg connection to the running Compose Postgres and read the seeded GXP-MFG-DEMO-01 row (EVID-01 substrate, D-01)."
    - "A backend test can drive llm_router.call_llm() against a respx-mocked Gemini generateContent response and receive a parsed LLMResponse carrying the real model_id (D-01 wire contract)."
    - "With no provider key present, call_llm() returns a degraded LLMResponse instead of raising, and no API key value appears in any emitted log record (D-01 degraded-mode-is-first-class)."
    - "With Postgres unreachable, acquire_pool_or_none() returns None instead of raising, so A2/C1 can reach their Bible-specified fallbacks."
  artifacts:
    - backend/app/db.py
    - backend/app/llm_router.py
    - backend/tests/test_db.py
    - backend/tests/test_llm_router.py
  key_links:
    - "llm_router.PROVIDER_CONFIG task-name routing -> the `use_for` lists A0 (`orchestrator`) and A2 (`compliance`) select on; a task name with no matching entry must fail loudly at selection time, not silently pick a default."
    - "db.DATABASE_URL -> the Compose Postgres credentials (user `sentinel`, db `sentinel`, 127.0.0.1:5432) established in Phase 1's docker-compose.yml; a mismatch here silently breaks every A2 and C1 evidence fetch downstream."
    - "call_llm()'s primary-provider failure -> `openrouter_fallback` cascade (Bible Section 8.2); if this cascade is skipped, every agent's own Bible fallback fires one layer too early."
---

<objective>
Build the two pieces of shared infrastructure Phase 3 has no substitute for and that do not exist anywhere in the backend today: a multi-provider LLM router (Bible Section 8) and real Postgres connectivity (asyncpg). Also extend `app/schemas.py` with the Bible Section 2 agent input/output models the later waves import, and add the four provider-key placeholders plus `DATABASE_URL` to `.env.example` per D-01.

Purpose: every downstream plan in this phase (the hero tracer, A0, A2, C1) calls into one or both of these modules. `grep -r "asyncpg|DATABASE_URL|db_pool" backend/` currently returns nothing — this is genuinely new scope, not a Phase 2 carryover (03-RESEARCH.md Pitfall 4).

Output: `backend/app/db.py`, `backend/app/llm_router.py`, extended `backend/app/schemas.py`, extended `backend/tests/conftest.py`, two new test modules, `.env.example` and `backend/requirements.txt` updates, and two new Bible-deviation entries in `backend/README.md`.

Not a tracer plan by design: this plan produces no user-visible capability slice — it is pure substrate for the `03-02` tracer, which is the phase's first end-to-end vertical slice. Splitting it out is what lets `03-02`'s tracer task stay inside the five-file bound while still touching every layer.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-intelligence-retrieval/03-CONTEXT.md
@.planning/phases/03-intelligence-retrieval/03-RESEARCH.md
@backend/app/opa_client.py
@backend/app/schemas.py
@backend/tests/conftest.py
@backend/tests/test_opa_client.py
@backend/README.md
@BRANCHING.md
</context>

## Source Coverage Audit (phase-wide — applies to all six plans)

CONTEXT.md records its decisions by name, not by `D-NN` id. The following stable ids are assigned once here and cited by every plan in this phase.

| ID | CONTEXT.md decision | Covered by |
|----|---------------------|------------|
| D-01 | Credential gap — build real production-shaped provider code; prove wire contract with respx; degraded-mode fallback is first-class, tested behavior; `.env.example` gains four key placeholders | 03-01, 03-02, 03-03, 03-04 |
| D-02 | A0 scope — Gemini 2.5 Flash intent classification, `Send` fan-out to a subset, hard `asyncio.wait_for` 2000ms fallback to the full six-agent set (not a sleep approximation) | 03-03 |
| D-03 | A2 scope — real `AgentFinding` from live DB via `verify_urs_approved` / `verify_periodic_eval_current` / `verify_test_traceability`; LLM narrates only, never evaluates the threshold | 03-02, 03-04 |
| D-04 | C1 scope — `calculate_confidence()` against real DB record + real OPA evaluation, never a mock; engineered contradiction fixture; Rule 6 unit + negative + edge + integration coverage | 03-02, 03-05 |
| D-05 | Resolved Open Question 1 — seed one additional minimal `documents` row with `doc_type='URS'` in an additive, idempotent seed script | 03-04 |
| D-06 | Resolved Open Question 2 — ALCOA constant is **9**, not the Bible's literal `8`; implement `(9 - alcoa_score) * 10`; record as a Bible deviation routed to SENT-7-05 | 03-02, 03-05 |
| D-07 | Claude's discretion — module layout under `backend/app/`; extend the existing `graph/state.py` skeleton rather than restructuring it; minimal-but-real A1/A3–A6 proportionate to their v2 status; retry/backoff policy | 03-01, 03-02, 03-03 |

| Source | Item | Status |
|--------|------|--------|
| GOAL | Real query enters A0, classified and routed, fans out to real (non-stub) agents, C1 produces a non-trivial confidence score from real DB + OPA | COVERED — 03-02 (tracer), 03-03 (A0 + specialists), 03-05 (C1), 03-06 (hero loop) |
| REQ | ORC-02 | COVERED — 03-03 |
| REQ | ORC-03 | COVERED — 03-02 (one check), 03-04 (all three) |
| REQ | EVID-01 | COVERED — 03-02 (wiring), 03-05 (hardening) |
| REQ | EVID-02 | COVERED — 03-05 |
| REQ | EVID-04 | COVERED — 03-02 (thin path), 03-06 (full loop) |
| RESEARCH | `llm_router.py` with `PROVIDER_CONFIG` + `openrouter_fallback` cascade | COVERED — 03-01 |
| RESEARCH | `db.py` asyncpg pool + `DATABASE_URL` (Pitfall 4) | COVERED — 03-01 |
| RESEARCH | respx wire-contract + forced-failure degraded tests (Pitfall 6) | COVERED — 03-01, 03-03, 03-04 |
| RESEARCH | DeepSeek `deepseek-reasoner` retirement + OpenRouter `openrouter/auto` corrections (Pitfalls 1, 3) | COVERED — 03-01 |
| RESEARCH | A2 query shapes grounded in schema + seeded gaps (Pitfall 5) | COVERED — 03-04 |
| RESEARCH | `calculate_confidence()` transcribed exactly | COVERED — 03-02 |
| RESEARCH | Degraded-Mode Fallback Contract table (per agent, Bible Section 2) | COVERED — 03-03 (A1, A3–A6), 03-04 (A2), 03-05 (C1) |
| RESEARCH | Minimal-but-real A1/A3–A6 as `Send` fan-out targets | COVERED — 03-03 |
| RESEARCH | Security domain — parameterized SQL, no key leakage in logs, explicit outbound timeouts, non-overridable `base_url` | COVERED — 03-01, 03-04 |
| RESEARCH | Package Legitimacy Audit — `asyncpg` / `respx` tagged `[ASSUMED]`, blocking checkpoint required | COVERED — 03-01 Task 1 |
| CONTEXT | D-01 … D-07 | COVERED — see table above |

No item is MISSING. Excluded as out of scope, per CONTEXT.md `<deferred>`: full A1 Qdrant RAG stack (SENT-2-08…2-11), full A3/A4/A5/A6 agents, live-LLM-quality verification.

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package legitimacy gate — asyncpg and respx</name>
  <read_first>
    - .planning/phases/03-intelligence-retrieval/03-RESEARCH.md ("Package Legitimacy Audit" section)
  </read_first>
  <action>
Two PyPI packages are about to be installed. 03-RESEARCH.md's audit could not run the automated legitimacy seam (gsd-tools is not present in this environment, exit 127), so both package names are tagged `[ASSUMED]` on name-provenance grounds and require human confirmation before install. Present the following to the developer and stop.

  - `asyncpg==0.31.0` — expected source repo `github.com/MagicStack/asyncpg`; expected PyPI page `pypi.org/project/asyncpg/`. This is the driver Bible Section 7.1's own `AuditLogger` example imports.
  - `respx==0.23.1` — expected source repo `github.com/lundberg/respx`; expected PyPI page `pypi.org/project/respx/`. Requires `httpx>=0.25.0`, satisfied by the already-pinned `httpx==0.28.1`.

Do not run `pip install` until the developer confirms. This checkpoint is not auto-approvable regardless of any auto-advance setting.
  </action>
  <verify>
    <human-check>Developer opens pypi.org/project/asyncpg/ and pypi.org/project/respx/, confirms each page's declared source repository matches the expected repo above, and replies with an explicit approval.</human-check>
  </verify>
  <acceptance_criteria>
    - The developer has explicitly approved both packages by name and version in the session transcript.
    - No `pip install` of either package has been executed before that approval.
  </acceptance_criteria>
  <done>Both packages are approved for install, or the plan halts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Postgres connectivity — asyncpg pool, DATABASE_URL wiring, and the shared db_pool test fixture</name>
  <files>backend/requirements.txt, .env.example, backend/app/db.py, backend/tests/conftest.py, backend/tests/test_db.py</files>
  <read_first>
    - backend/app/opa_client.py (the house style this module mirrors: module-level env-var-backed constant, `logger` not `print`, degrade-don't-raise)
    - backend/tests/conftest.py (existing `client`, `opa_base_url`, `pinned_now_ns` fixtures — extend, do not restructure)
    - backend/tests/test_opa_client.py (the `asyncio.run()`-inside-a-plain-`def`-test convention this suite uses; pytest-asyncio is deliberately absent)
    - backend/README.md ("Local setup (Stage 1)" — installs go through `backend/.venv/Scripts/python -m pip`, never a bare `pip`)
    - docker-compose.yml (postgres service: user `sentinel`, db `sentinel`, published on `127.0.0.1:5432`)
    - infra/postgres/seed/001_seed.sql (the seeded `GXP-MFG-DEMO-01` row this task's live test reads)
    - BRANCHING.md section 5 (`.env.example` is a shared file; this plan is its sole toucher in wave 1)
  </read_first>
  <behavior>
    - `get_pool()` returns an `asyncpg.Pool` connected to the live Compose Postgres, and a second call returns the same object (singleton).
    - A real query through that pool for `GXP-MFG-DEMO-01` returns a row whose `name` is `NovaSynth Manufacturing Execution Support System` and whose `readiness_score` is `61`.
    - `acquire_pool_or_none()` with `DATABASE_URL` pointed at a closed port returns `None`, logs a warning on the `app.db` logger, and does not raise.
    - `close_pool()` is idempotent — calling it twice, or calling it when no pool was ever created, does not raise.
    - After `close_pool()`, a subsequent `get_pool()` re-reads the current module-level `DATABASE_URL` rather than reusing the stale one.
  </behavior>
  <action>
Add `asyncpg==0.31.0` and `respx==0.23.1` to `backend/requirements.txt`, preserving the file's existing one-package-per-line pinned style, then install with `backend/.venv/Scripts/python -m pip install -r backend/requirements.txt` (never a bare `pip` — see backend/README.md).

Append to `.env.example`, below the existing Postgres block and separated by a blank line, a commented block followed by five empty-valued placeholder keys: `GEMINI_API_KEY=`, `DEEPSEEK_API_KEY=`, `GROQ_API_KEY=`, `OPENROUTER_API_KEY=`, and `DATABASE_URL=postgresql://sentinel:changeme@127.0.0.1:5432/sentinel`. The comment block states that the four provider keys are required only for live LLM calls, that the system runs in an explicit degraded/abstain mode without them, and that `DATABASE_URL`'s password segment must match `POSTGRES_PASSWORD` in the operator's own `.env`. Per D-01 these are placeholders — never write a real or guessed key value.

Create `backend/app/db.py` exporting exactly:

  - `DATABASE_URL: str` — module-level, `os.getenv("DATABASE_URL", "postgresql://sentinel:sentinel@127.0.0.1:5432/sentinel")`. Read at call time inside `get_pool()`, not captured at import, so a test can `monkeypatch.setattr` it. Mirror `opa_client.OPA_URL`'s 127.0.0.1-not-localhost rationale.
  - `async def get_pool() -> asyncpg.Pool` — lazily creates a module-level singleton via `asyncpg.create_pool(DATABASE_URL, min_size=1, max_size=5, timeout=5.0, command_timeout=10.0)` and returns it. Propagates connection failures to the caller.
  - `async def acquire_pool_or_none() -> asyncpg.Pool | None` — wraps `get_pool()` in `try/except (OSError, asyncpg.PostgresError, asyncio.TimeoutError)`, logs `logger.warning` including the target host and the exception text, returns `None`. This is the entry point every agent uses, so an unreachable database routes each agent to its Bible-specified fallback instead of crashing the graph.
  - `async def close_pool() -> None` — closes and clears the singleton; safe to call when none exists.

Use `logging.getLogger(__name__)`, never `print`. Every SQL statement written anywhere in this phase uses asyncpg's `$1`-style placeholders exclusively — `system_id` originates from user-facing input (ASVS V5).

Extend `backend/tests/conftest.py` with a session-scoped `db_pool` fixture that yields the result of `asyncio.run(get_pool())` and calls `asyncio.run(close_pool())` on teardown, and a function-scoped `reset_db_pool` fixture that calls `close_pool()` before and after the test so URL-override tests start clean. Add nothing else; keep the existing three fixtures untouched.

Write `backend/tests/test_db.py` covering the five behaviors above, using `asyncio.run()` inside plain `def test_*` functions.
  </action>
  <verify>
    <automated>cd backend &amp;&amp; .venv/Scripts/python -m pytest tests/test_db.py -x -q</automated>
    <automated>cd backend &amp;&amp; .venv/Scripts/python -c "import asyncpg, respx; print(asyncpg.__version__)"</automated>
    <automated>cd backend &amp;&amp; grep -v '^\s*#' app/db.py | grep -c 'logger\.' | awk '$1 &gt;= 1 {exit 0} {exit 1}'</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_db.py -x -q` exits 0 with at least 5 passing tests.
    - `grep -c 'asyncpg==0.31.0' backend/requirements.txt` outputs `1` and `grep -c 'respx==0.23.1' backend/requirements.txt` outputs `1`.
    - `grep -c '^GEMINI_API_KEY=$' .env.example`, `grep -c '^DEEPSEEK_API_KEY=$' .env.example`, `grep -c '^GROQ_API_KEY=$' .env.example`, and `grep -c '^OPENROUTER_API_KEY=$' .env.example` each output `1` — the four keys are present and each has an empty value.
    - `grep -c '^DATABASE_URL=postgresql://' .env.example` outputs `1`.
    - Stripping comment lines, `backend/app/db.py` contains zero occurrences of the token `print(`: `grep -v '^\s*#' backend/app/db.py | grep -c 'print(' ` outputs `0`.
    - Stripping comment lines, every SQL string literal in `backend/app/db.py` and `backend/tests/test_db.py` that interpolates a system id does so via a `$1` placeholder — verified by `grep -v '^\s*#' backend/app/db.py backend/tests/test_db.py | grep -c 'f"SELECT'` outputting `0`.
    - `backend/tests/conftest.py` still defines `client`, `opa_base_url`, and `pinned_now_ns` (each `grep -c 'def client'` style check outputs `1`).
  </acceptance_criteria>
  <done>A backend test opens a real connection to the running Compose Postgres, reads the seeded `GXP-MFG-DEMO-01` row, and the unreachable-database path returns `None` with a logged warning instead of raising.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Multi-provider LLM router — PROVIDER_CONFIG, call_llm(), openrouter cascade, and the two Bible corrections</name>
  <files>backend/app/llm_router.py, backend/app/schemas.py, backend/tests/test_llm_router.py, backend/README.md</files>
  <read_first>
    - .planning/phases/03-intelligence-retrieval/03-RESEARCH.md ("Code Examples" — the transcribed `PROVIDER_CONFIG`; Pitfall 1 DeepSeek; Pitfall 2 Groq/Gemini currency; Pitfall 3 OpenRouter; "Architecture Patterns" Pattern 3 respx; "Security Domain")
    - AegisX-AI-Project-Bible-v6.md Section 8 (lines 1187-1243 — 8.1 `PROVIDER_CONFIG`, 8.2 router logic and the `openrouter_fallback` cascade)
    - AegisX-AI-Project-Bible-v6.md Section 2 (lines 232-244 — A0's `OrchestratorInput` / `OrchestratorOutput`; A2's `ComplianceInput`)
    - backend/app/opa_client.py (async-httpx call shape, explicit `timeout=`, degrade-and-log-don't-raise)
    - backend/app/schemas.py (existing Pydantic models; append, do not reorder or restyle)
    - backend/README.md section "Bible deviations (backend tier)" (the established deviation-entry format: **Bible says** / **Implemented** / **Why** / **Evidence** / **Scope**)
  </read_first>
  <behavior>
    - `select_provider("orchestrator")` returns `"gemini_flash_thinking"`; `select_provider("compliance")` returns `"gemini_flash_fast"`; `select_provider("fallback")` returns `"openrouter_fallback"`; `select_provider("no_such_task")` raises `KeyError`.
    - Against a respx-mocked Gemini `generateContent` 200 whose body is `{"candidates":[{"content":{"parts":[{"text":"..."}]}}]}`, `call_llm(task="orchestrator", ...)` returns an `LLMResponse` whose `text` is that inner string, whose `model_id` is `gemini-2.5-flash`, and whose `degraded` is `False`.
    - The outgoing Gemini request carries the key in an `x-goog-api-key` header and a JSON body containing `contents`, and its `generationConfig.thinkingConfig.thinkingBudget` equals the selected provider entry's `thinking_budget` (512 for `orchestrator`, 0 for `compliance`).
    - Against a respx-mocked OpenAI-compatible `chat/completions` 200 whose body is `{"choices":[{"message":{"content":"..."}}],"model":"..."}`, `call_llm(task="incident", ...)` returns the inner content and the model string the provider echoed back.
    - A primary-provider 429 with the OpenRouter key present routes a second request to `openrouter.ai/api/v1/chat/completions`, and the returned `LLMResponse.model_id` reflects the OpenRouter response, not the primary's.
    - A primary-provider 500 and a primary-provider `httpx.ReadTimeout` each trigger the same cascade.
    - With every provider key absent from the environment, `call_llm()` returns `LLMResponse(text="", degraded=True)` with a non-empty `failure_reason`, raises nothing, and issues zero outbound HTTP requests.
    - No log record emitted during any failure path contains the literal key value that was set in the environment for that test.
  </behavior>
  <action>
Create `backend/app/llm_router.py`.

Transcribe `PROVIDER_CONFIG` from Bible Section 8.1 with exactly two corrections, both already researched and both to be recorded as deviations in this same task: `deepseek_r1["model"]` becomes `deepseek-v4-pro` (the Bible's value is retired per 03-RESEARCH.md Pitfall 1), and `openrouter_fallback["model"]` becomes `openrouter/auto` (Pitfall 3). Every other key — provider, base_url, api_key_env, rpm_limit, thinking_budget, use_for — is carried over unchanged. `gemini_flash_thinking.use_for` is `["orchestrator", "synthesis", "remediation"]`; `gemini_flash_fast.use_for` is `["compliance", "knowledge", "change"]`.

Resolve the key-name conflict between the Bible (`api_key_env: "GOOGLE_API_KEY"`) and D-01 (`.env.example` gains `GEMINI_API_KEY=`) by having the Google entries read `GEMINI_API_KEY` first and accept `GOOGLE_API_KEY` as a documented alias when the first is unset. Both entries stay in `PROVIDER_CONFIG` as an explicit tuple/list of accepted env-var names. Record this as the third deviation.

`base_url` values are hardcoded module constants inside `PROVIDER_CONFIG`. Nothing in `AgentState`, in an incoming query, or in any environment variable may redirect them (ASVS SSRF control from 03-RESEARCH.md's Security Domain).

Define a Pydantic model `LLMResponse` in `app/llm_router.py` with fields `text: str`, `model_id: str`, `provider: str`, `degraded: bool = False`, `failure_reason: Optional[str] = None`. Section 8.2 requires the actually-used model id to ride on the response for auditability, so `model_id` is never blank on a non-degraded response.

Define `select_provider(task: str) -> str` returning the single `PROVIDER_CONFIG` key whose `use_for` contains `task`, raising `KeyError` naming the unknown task otherwise. No silent default — a typo in a task name must fail loudly at selection time.

Define `async def call_llm(task: str, prompt: str, system_instruction: str = "", timeout: float = 10.0, json_output: bool = False) -> LLMResponse`. Two request builders, selected by the entry's `provider` field:

  - Google: `POST {base_url}/models/{model}:generateContent`, header `x-goog-api-key`, body `{"contents": [{"parts": [{"text": prompt}]}]}` plus `systemInstruction` when supplied, plus `generationConfig` carrying `thinkingConfig.thinkingBudget` from the entry and `responseMimeType` of `application/json` when `json_output` is set. Response text is read from `candidates[0].content.parts[0].text`.
  - OpenAI-compatible (deepseek, groq, openrouter): `POST {base_url}/chat/completions`, header `Authorization` with a bearer token, body `{"model": ..., "messages": [...]}`. Response text is read from `choices[0].message.content`; `model_id` is read from the response's own `model` field, falling back to the configured model string.

Every outbound call passes an explicit `timeout=` to `httpx` (ASVS V10). Failure handling, in order: a missing key for the selected provider, an `httpx.TimeoutException`, an `httpx.HTTPStatusError` with status 429, and any 5xx all cascade once to `openrouter_fallback` per Section 8.2; if the cascade target is itself unusable, return a degraded `LLMResponse` rather than raising. `call_llm()` raises no exception to its caller under any of these conditions — that is what makes each agent's own Bible fallback the last resort rather than the first.

Log failures with `logging.getLogger(__name__).warning` recording only the provider name, the request URL host, the status code, and a truncated response body. Never log the `httpx.Request` object, the request headers, or any environment value, because an httpx request carries the bearer token in its headers (ASVS V7 / Information Disclosure). Take no retry beyond the single cascade — a slow provider is A0's fallback trigger, not a retry trigger (03-RESEARCH.md "Don't Hand-Roll").

No environment-flag branch may short-circuit request construction to return a canned string; the mocked transport lives only in the test layer (D-01).

Append to `backend/app/schemas.py`, at the end and in the existing style, three Bible Section 2 models: `OrchestratorInput(user_query: str, system_id: str)`, `OrchestratorOutput(active_agents: List[str], intent_category: str)`, and `ComplianceInput(system_id: str)`. Field names and types are transcribed from Section 2 verbatim.

Write `backend/tests/test_llm_router.py` covering all eight behaviors above with `@respx.mock` and `asyncio.run()` inside plain `def test_*` functions, using `monkeypatch.setenv`/`monkeypatch.delenv` to control key presence. The key-leakage test sets a recognizable sentinel key value, captures `caplog` across a forced failure, and asserts that value appears in no record's message or arguments.

Append three entries to `backend/README.md` under "Bible deviations (backend tier)", numbered to continue the existing sequence (Deviation 4, 5, 6), each following the established **Bible says** / **Implemented** / **Why** / **Evidence** / **Scope** structure, and each routed to SENT-7-05: the DeepSeek model correction, the OpenRouter model-string correction, and the Gemini key-env-var alias.
  </action>
  <verify>
    <automated>cd backend &amp;&amp; .venv/Scripts/python -m pytest tests/test_llm_router.py -x -q</automated>
    <automated>cd backend &amp;&amp; .venv/Scripts/python -m pytest tests/test_schemas.py -x -q</automated>
    <automated>cd backend &amp;&amp; .venv/Scripts/python -c "from app.llm_router import PROVIDER_CONFIG, select_provider; assert select_provider('orchestrator')=='gemini_flash_thinking'; assert select_provider('compliance')=='gemini_flash_fast'; assert PROVIDER_CONFIG['gemini_flash_thinking']['thinking_budget']==512; assert PROVIDER_CONFIG['gemini_flash_fast']['thinking_budget']==0; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_llm_router.py -x -q` exits 0 with at least 8 passing tests, one per listed behavior.
    - `python -c "from app.llm_router import PROVIDER_CONFIG; print(sorted(PROVIDER_CONFIG))"` prints exactly `['deepseek_r1', 'gemini_flash_fast', 'gemini_flash_thinking', 'groq_llama', 'openrouter_fallback']`.
    - `python -c "from app.llm_router import PROVIDER_CONFIG as p; print(p['deepseek_r1']['model'], p['openrouter_fallback']['model'], p['groq_llama']['model'])"` prints `deepseek-v4-pro openrouter/auto llama-3.3-70b-versatile`.
    - `python -c "from app.schemas import OrchestratorInput, OrchestratorOutput, ComplianceInput; print('ok')"` prints `ok`.
    - Stripping comment lines, `backend/app/llm_router.py` contains zero occurrences of `print(`: `grep -v '^\s*#' backend/app/llm_router.py | grep -c 'print('` outputs `0`.
    - `backend/README.md` contains three new headings matching `^### Deviation 4`, `^### Deviation 5`, and `^### Deviation 6`, and the string `SENT-7-05` appears at least 4 times in the file.
    - The key-leakage test is present and named so that `pytest tests/test_llm_router.py -k leak -q` selects at least one test and exits 0.
  </acceptance_criteria>
  <done>`call_llm()` parses a respx-mocked Gemini response into a typed `LLMResponse` carrying `gemini-2.5-flash`, cascades to OpenRouter on 429/5xx/timeout, returns a degraded response with no key present instead of raising, and never emits a key value into a log record.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| backend process -> external LLM provider APIs | Outbound requests carry a bearer credential; responses are untrusted external data parsed into typed models |
| backend process -> Postgres | `system_id` values originating from user-facing input reach SQL statements across this boundary |
| process environment -> log sink | Credentials live in the environment; log records are written to a lower-trust sink |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Information Disclosure | `llm_router.call_llm()` failure logging | high | mitigate | Log only provider name, URL host, status code, truncated body; never log the `httpx.Request` object or headers. Proven by the `-k leak` test asserting a sentinel key value appears in no `caplog` record (Task 3). |
| T-03-02 | Tampering (SSRF) | `PROVIDER_CONFIG["*"]["base_url"]` | medium | mitigate | `base_url` values are hardcoded module constants; no `AgentState` field, query string, or environment variable can redirect them (Task 3). |
| T-03-03 | Tampering (SQL injection) | `db.py` and every downstream agent query | high | mitigate | asyncpg `$1` placeholders exclusively; no f-string or `%`-formatted SQL. Gated by the `grep -c 'f"SELECT'` check outputting `0` (Task 2). |
| T-03-04 | Denial of Service | Outbound provider calls | medium | mitigate | Every `httpx` call passes an explicit `timeout=`; the cascade fires once with no retry loop (Task 3). |
| T-03-05 | Spoofing | Untrusted provider response body | medium | mitigate | Every response is parsed into the `LLMResponse` Pydantic model before entering application state; a malformed body yields a degraded response, never an unvalidated dict (Task 3). |
| T-03-06 | Information Disclosure | `.env.example` | low | accept | Placeholders carry empty values only; no real or guessed key is ever written (D-01). Accepted because the file is committed by design and contains no secret. |
| T-03-SC | Tampering | pip installs (`asyncpg`, `respx`) | high | mitigate | Package-legitimacy gate plus the blocking human checkpoint in Task 1 for both `[ASSUMED]` packages. |
</threat_model>

<verification>
Run from the repository root with Postgres and OPA already up (`docker compose up -d --wait`):

```bash
bash infra/health-check.sh
cd backend && .venv/Scripts/python -m pytest -q
```

The full suite must stay green — this plan adds two test modules and must break none of Phase 1/2's existing schema, topology, OPA, health, or WebSocket tests.

Wire contract vs. live quality (03-RESEARCH.md Pitfall 6): everything verified here proves request construction and response parsing against provider-shaped mocked responses, plus the degraded path. It does **not** prove that a live Gemini call returns useful content. That remains the operator follow-up recorded in `user_setup`.
</verification>

<success_criteria>
- `backend/app/db.py` and `backend/app/llm_router.py` exist and are importable with no side effects at import time.
- `pytest tests/test_db.py tests/test_llm_router.py -q` exits 0.
- The full backend suite exits 0.
- `.env.example` carries five new placeholder keys, all with empty or non-secret values.
- Three new Bible deviations are recorded in `backend/README.md` and routed to SENT-7-05.
</success_criteria>

## Artifacts this phase produces

Produced by **this plan** (03-01):

| Artifact | Symbols |
|----------|---------|
| `backend/app/db.py` | `DATABASE_URL`, `get_pool()`, `acquire_pool_or_none()`, `close_pool()` |
| `backend/app/llm_router.py` | `PROVIDER_CONFIG`, `LLMResponse`, `select_provider()`, `call_llm()` |
| `backend/app/schemas.py` (extended) | `OrchestratorInput`, `OrchestratorOutput`, `ComplianceInput` |
| `backend/tests/conftest.py` (extended) | `db_pool` fixture, `reset_db_pool` fixture |
| `backend/tests/test_db.py` | 5 tests |
| `backend/tests/test_llm_router.py` | 8+ tests including the key-leakage test |
| `backend/requirements.txt` (extended) | `asyncpg==0.31.0`, `respx==0.23.1` |
| `.env.example` (extended) | `GEMINI_API_KEY`, `DEEPSEEK_API_KEY`, `GROQ_API_KEY`, `OPENROUTER_API_KEY`, `DATABASE_URL` |
| `backend/README.md` (extended) | Deviations 4, 5, 6 |

Later plans in this phase add: `backend/app/agents/` (`a0_orchestrator.py`, `a2_compliance.py`, `c1_verifier.py`, `minimal_specialists.py`), the three A2 verification functions, `calculate_confidence()`, `infra/postgres/seed/002_urs_fixture.sql`, and the `test_hero_tracer.py` / `test_a0_orchestrator.py` / `test_a2_compliance.py` / `test_c1_verifier.py` / `test_hero_loop.py` suites.

<output>
Create `.planning/phases/03-intelligence-retrieval/03-01-SUMMARY.md` when done.
</output>
