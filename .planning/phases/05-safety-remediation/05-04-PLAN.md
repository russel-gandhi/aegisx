---
phase: 05-safety-remediation
plan: 04
type: tdd
wave: 2
depends_on: ["05-01"]
files_modified:
  - backend/app/agents/c3_gateway.py
  - backend/app/agents/a7_remediation.py
  - backend/app/routes/actions.py
  - backend/tests/test_c3_gateway.py
  - backend/tests/test_a7_remediation.py
  - backend/tests/test_routes_actions.py
autonomous: true
requirements: [REM-01, REM-02, REM-03]
user_setup: []

estimate:
  tokens: 58000
  raw_tokens: 58000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Every one of the Bible's five action categories is reachable from a named action_type, and an unrecognised action_type resolves to PROHIBITED."
    - "A PROHIBITED proposal never reaches action_proposals and is recorded as a blocked-attempt audit event instead."
    - "A7 produces no proposal for a finding whose C1 confidence is INSUFFICIENT_EVIDENCE, and produces one for HIGH, MEDIUM, and LOW."
    - "A7 never calls C1's verifier — proven by an AST check on the module's imports, not by reading it."
    - "A human can reject a pending proposal; it reaches REJECTED, is audit-logged, and can never afterwards be approved."
    - "Every field of the approval-queue response is traceable to a database column or to the frozen category allowlist — no field is generated at render time."
  artifacts:
    - backend/tests/test_c3_gateway.py
    - backend/tests/test_a7_remediation.py
  key_links:
    - "route_action -> QUEUED_CATEGORIES gate: only MOCK_WRITE_LOW_RISK and GXP_RELEVANT_WRITE reach persist_proposal"
    - "synthesize_capa -> verification_result['confidence'] (never finding['confidence_score'], which A2 fixes at UNVERIFIED)"
    - "reject route -> status guard -> log_event(action_type='ACTION_REJECTED')"
---

<objective>
Bring C3 and A7 to the Critical-review bar REM-01/REM-02 require, and complete the human decision surface with a reject path: all five Bible categories routed and tested, PROHIBITED blocked and recorded rather than queued, A7 provably synthesising only from C1-verified findings, and a proposal a human declines reaching a terminal REJECTED state.

Purpose: C3 is a Critical-review ticket under CLAUDE.md Rule 6, and REM-01's "never unverified claims" is the remediation-side restatement of the product's whole thesis — it needs a mechanical guard, not a comment. The reject path closes 05-RESEARCH.md Open Question 3, which UI-SPEC.md already styles and which the plan therefore confirms as in scope.
Output: full C3 and A7 test suites, the blocked-attempt audit path, and `POST /api/actions/{proposal_id}/reject`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/05-safety-remediation/05-CONTEXT.md
@.planning/phases/05-safety-remediation/05-RESEARCH.md
@.planning/phases/05-safety-remediation/05-UI-SPEC.md
@.planning/phases/05-safety-remediation/05-01-SUMMARY.md
@backend/app/agents/c3_gateway.py
@backend/app/agents/a7_remediation.py
@backend/app/routes/actions.py
</context>

<artifacts>
## Artifacts this plan produces

**New files**
`backend/tests/test_c3_gateway.py`, `backend/tests/test_a7_remediation.py`

**New symbols**
- `c3_gateway.py`: `BLOCKED_CATEGORIES`, `describe_category`
- `routes/actions.py`: `POST /api/actions/{proposal_id}/reject`
- Extended: `c3_gateway.ACTION_CATEGORIES` (all five categories represented), `a7_remediation.synthesize_capa` (CAPA field completeness)
</artifacts>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: C3 Critical-review coverage — five categories, fail-closed default, blocked-attempt audit path</name>
  <files>backend/app/agents/c3_gateway.py, backend/tests/test_c3_gateway.py</files>
  <read_first>
    - `backend/app/agents/c3_gateway.py` (as 05-01 left it) — `ACTION_CATEGORIES`, `QUEUED_CATEGORIES`, `route_action`, `persist_proposal`, and the docstring paragraph reconciling Bible Section 2's two GXP_RELEVANT_WRITE statements
    - `AegisX-AI-Project-Bible-v6.md` lines 362-371 — C3's role, its five categories with their one-line dispositions, and the workflow line
    - `backend/app/agents/c1_verifier.py` lines 76-155 — the frozen-allowlist convention and the no-f-string SQL discipline `persist_proposal` follows
    - `.planning/phases/05-safety-remediation/05-RESEARCH.md` — Assumptions Log row A5 (whether PROHIBITED should still be audit-logged)
    - `CLAUDE.md` Rule 6 — the Critical-review coverage bar
  </read_first>
  <behavior>
    - `route_action` returns `READ`, `DRAFT`, `MOCK_WRITE_LOW_RISK`, `GXP_RELEVANT_WRITE`, and `PROHIBITED` for the respective named action types — all five Bible categories are reachable.
    - `route_action("")`, `route_action("unknown-type")`, and `route_action("create_capa_record")` (wrong case) all return `PROHIBITED`.
    - `QUEUED_CATEGORIES` contains exactly `MOCK_WRITE_LOW_RISK` and `GXP_RELEVANT_WRITE`.
    - `BLOCKED_CATEGORIES` contains `PROHIBITED`, and `PROHIBITED` is absent from `QUEUED_CATEGORIES`.
    - `persist_proposal` writes exactly one row with `status = "PENDING_APPROVAL"` and the supplied `justification`, `finding_id`, `session_id`, and `model_id`.
    - Two `persist_proposal` calls in the same run produce distinct ids that sort in creation order.
    - `describe_category` returns Bible Section 2's own one-line disposition text for each of the five categories, and raises `KeyError` for anything else.
  </behavior>
  <action>
Extend `backend/app/agents/c3_gateway.py`:
- Add `BLOCKED_CATEGORIES: FrozenSet[str] = frozenset({"PROHIBITED"})` with a comment that `READ` and `DRAFT` are auto-executing per Bible Section 2 and are therefore neither queued nor blocked — they are simply not reachable from anything A7 emits this phase, which is why this phase's write surface only ever sees the queued and blocked sets.
- Add `CATEGORY_DISPOSITIONS: Dict[str, str]` and `def describe_category(category: str) -> str`, mapping each of the five categories to Bible Section 2's own one-line disposition sentence, transcribed verbatim ("Automatic execution.", "Saved to local state, automatic execution.", "Sent to Human Approval Queue.", "Blocked. Requires out-of-band execution.", "Blocked immediately."). This is what the approval UI displays as the server-trusted explanation of the category, so it must come from the Bible's own text rather than from a client-side lookup table. `describe_category` raises `KeyError` for an unknown category rather than returning a placeholder.
- Verify `ACTION_CATEGORIES` covers all five categories after 05-01; if any category has no `action_type` mapped to it, add a named one following the existing style so `set(ACTION_CATEGORIES.values())` is exactly the five Bible categories.

Create `backend/tests/test_c3_gateway.py` covering the `<behavior>` list, plus these Critical-review additions:
- `test_all_five_bible_categories_are_reachable`: `set(ACTION_CATEGORIES.values())` equals the literal five-element set built in the test from Bible Section 2, so a dropped or invented category fails here.
- `test_unknown_action_type_fails_closed_to_prohibited`: parametrised across empty string, whitespace, a wrong-case variant, and a plausible-but-unmapped type. Its docstring records that returning `READ` (the permissive category) on an unknown type would be the dangerous default, which is why the fallback is `PROHIBITED`.
- `test_c3_module_has_no_model_dependency`: the same AST-based gate 05-02 uses for C2 — parse `backend/app/agents/c3_gateway.py`, walk `ast.Import`/`ast.ImportFrom`, and assert no imported module name contains `llm` and no imported name is `call_llm`. Bible Section 1.3 makes this permanent for C3 as it does for C2.
- `test_persist_proposal_binds_every_value` (live Postgres): call `persist_proposal` with an `action_type` containing a SQL metacharacter payload, read the row back, and assert the stored `action_type` is that exact literal string and that `SELECT count(*) FROM action_proposals` increased by exactly one — proving the value was bound, not interpolated. Use the `audit_chain_isolation`-style cleanup pattern with a module fixture deleting the ids it created.
- `test_proposal_ids_sort_in_creation_order`.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_c3_gateway.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && python -m pytest tests/test_c3_gateway.py -x` exits 0.
    - `python -c "from app.agents.c3_gateway import ACTION_CATEGORIES as a; print(sorted(set(a.values())))"` prints `['DRAFT', 'GXP_RELEVANT_WRITE', 'MOCK_WRITE_LOW_RISK', 'PROHIBITED', 'READ']`.
    - `python -c "from app.agents.c3_gateway import route_action as r; print(r(''), r('unknown-type'))"` prints `PROHIBITED PROHIBITED`.
    - Tests named `test_all_five_bible_categories_are_reachable`, `test_unknown_action_type_fails_closed_to_prohibited`, `test_c3_module_has_no_model_dependency`, and `test_persist_proposal_binds_every_value` all exist and pass.
    - `python -c "from app.agents.c3_gateway import QUEUED_CATEGORIES as q; print('PROHIBITED' in q)"` prints `False`.
  </acceptance_criteria>
  <done>All five Bible categories are reachable and asserted, an unrecognised action type fails closed to PROHIBITED, category dispositions come from the Bible's own text, and an AST gate proves C3 imports no model client.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: A7 Critical-review coverage — verified-findings-only, CAPA completeness, degraded mode</name>
  <files>backend/app/agents/a7_remediation.py, backend/tests/test_a7_remediation.py</files>
  <read_first>
    - `backend/app/agents/a7_remediation.py` (as 05-01 left it) — `A7_ELIGIBLE_CONFIDENCE`, `synthesize_capa`, `_deterministic_capa`, and the docstring paragraph forbidding re-verification
    - `backend/app/routes/findings.py` lines 36-69 — `_assemble_card`'s docstring warning that `verification_result["confidence"]` and `finding["confidence_score"]` are adjacent, similarly named, and that reading the wrong one is the most likely defect
    - `backend/app/agents/a2_compliance.py` `narrate_gap` / `build_finding` — the LLM-narration + deterministic-fallback shape and the fixed `"UNVERIFIED"` literal A2 writes into `confidence_score`
    - `AegisX-AI-Project-Bible-v6.md` lines 314-323 (A7 contract) and 1085-1105 (A7 system prompt: root cause, corrective, preventive, due date 30 days, regulatory citation)
    - `backend/app/schemas.py` lines 124-131 — the `CAPAProposal` model's six required fields
    - `backend/tests/test_a2_compliance.py` — how this suite fakes `call_llm` without a live provider key
    - `.planning/phases/05-safety-remediation/05-CONTEXT.md` D-03 and `<code_context>` "Integration Points"
  </read_first>
  <behavior>
    - `synthesize_capa` returns `(None, ...)` when `verification_result["confidence"]` is `"INSUFFICIENT_EVIDENCE"`.
    - It returns a proposal dict for each of `"HIGH"`, `"MEDIUM"`, and `"LOW"`.
    - It returns `(None, ...)` for an unrecognised confidence value such as `"UNVERIFIED"` or an empty string — fail closed, never treat an unknown grade as eligible.
    - A returned proposal always carries a non-empty `justification`, an `action_type` present in `ACTION_CATEGORIES`, a `target_system` equal to the system it was asked about, and a `payload` containing the finding's `regulatory_citations` and `evidence_ids`.
    - The returned `payload` carries all six `CAPAProposal` fields, with `due_date` exactly 30 days after the synthesis date.
    - When the router degrades, a proposal is still returned and the model id is the literal `"deterministic-fallback"`.
    - The synthesis prompt embeds the finding's claim labelled as untrusted data to summarise, not as instructions to follow.
  </behavior>
  <action>
Extend `backend/app/agents/a7_remediation.py` so the returned proposal's `payload` is a complete CAPA structure rather than a free-form dict: `payload["capa"]` carries the six `CAPAProposal` fields (`root_cause`, `corrective_action`, `preventive_action`, `effectiveness_check`, `due_date`, `owner`), and `payload["regulatory_citations"]` and `payload["evidence_ids"]` are copied from the finding. Compute `due_date` deterministically in Python as `datetime.now(timezone.utc) + timedelta(days=30)` — Bible Section 6's A7 prompt asks the model for a 30-day due date, and a date is a computable fact, so it is computed rather than parsed out of prose. Record that choice in the function docstring, noting that it keeps a model from ever authoring a compliance deadline (Bible Section 1.3's spirit applied to a value the decision table does not enumerate). The model's contribution is the narrative fields; every structural and date field is server-computed.

Add explicit fail-closed handling: any `confidence` value not in `A7_ELIGIBLE_CONFIDENCE` returns `(None, "not-eligible")`, including `"UNVERIFIED"` and the empty string. Add a comment naming the adjacent-field trap: A2 writes the fixed literal `"UNVERIFIED"` into `finding["confidence_score"]` for every finding it emits, so a function reading that field instead of C1's `verification_result["confidence"]` would reject everything — and a future edit inverting the test would accept everything unverified, which is the REM-01 violation this guard exists to prevent.

Create `backend/tests/test_a7_remediation.py` covering the `<behavior>` list, faking `call_llm` the way `tests/test_a2_compliance.py` already does so no provider key is needed. Critical-review additions:
- `test_a7_never_imports_the_verifier`: AST-based, mirroring the C2/C3 gates — parse `backend/app/agents/a7_remediation.py`, walk `ast.Import`/`ast.ImportFrom`, and assert no import resolves to `app.agents.c1_verifier` and no imported name is `verify_finding` or `calculate_confidence`. Its docstring records REM-01: A7 reads C1's already-computed verdict and must never duplicate C1's authority.
- `test_a7_is_the_only_module_permitted_a_model_call_in_this_phase`: assert `a7_remediation` does import `call_llm`, as the positive counterpart to the C2/C3 gates — this documents the deterministic-first boundary from both sides.
- `test_capa_due_date_is_exactly_thirty_days_out`.
- `test_degraded_router_still_produces_a_proposal_with_fallback_attribution`.
- `test_prompt_labels_finding_text_as_untrusted`: capture the prompt passed to the faked `call_llm` and assert it contains the finding's claim and a phrase marking it as data to summarise rather than instructions.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_a7_remediation.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && python -m pytest tests/test_a7_remediation.py -x` exits 0.
    - Tests named `test_a7_never_imports_the_verifier`, `test_capa_due_date_is_exactly_thirty_days_out`, `test_degraded_router_still_produces_a_proposal_with_fallback_attribution`, and `test_prompt_labels_finding_text_as_untrusted` all exist and pass.
    - A parametrised test asserts `(None, ...)` for each of `"INSUFFICIENT_EVIDENCE"`, `"UNVERIFIED"`, `""`, and `"MAYBE"`, and a proposal for each of `"HIGH"`, `"MEDIUM"`, `"LOW"`.
    - `python -c "from app.agents.a7_remediation import A7_ELIGIBLE_CONFIDENCE as e; print(sorted(e))"` prints `['HIGH', 'LOW', 'MEDIUM']`.
    - A test asserts the returned `payload["capa"]` has exactly the six `CAPAProposal` field names.
  </acceptance_criteria>
  <done>A7 provably synthesises only from C1-verified findings, fails closed on any unrecognised grade, computes the CAPA structure and due date server-side, degrades to a deterministic narrative with honest attribution, and an AST gate proves it never re-runs verification.</done>
</task>

<task type="auto">
  <name>Task 3: Reject path and the server-trusted response guarantee</name>
  <files>backend/app/routes/actions.py, backend/tests/test_routes_actions.py</files>
  <read_first>
    - `backend/app/routes/actions.py` (as 05-01 left it) — the approve route's RBAC-first, pool guard, 404, 409 status-guard, update, `log_event` order
    - `backend/app/agents/c3_gateway.py` (as Task 1 left it) — `BLOCKED_CATEGORIES`, `describe_category`
    - `backend/app/schemas.py` — `ActionProposalRecord`, `ActionProposalsResponse`, `GenerateCapaResponse`
    - `.planning/phases/05-safety-remediation/05-UI-SPEC.md` — the "Destructive confirmation" copy row and the five-status badge palette (`PENDING_APPROVAL`, `APPROVED`, `EXECUTED`, `REJECTED`, `BLOCKED`)
    - `.planning/phases/05-safety-remediation/05-CONTEXT.md` D-02 (no auto-execution path exists anywhere)
    - `.planning/phases/05-safety-remediation/05-RESEARCH.md` Open Question 3
  </read_first>
  <precondition>Postgres is running with Phase 2 seed data loaded, so `generate-capa` against `GXP-MFG-DEMO-01` produces a real pending proposal for the reject test to act on.</precondition>
  <action>
Add `POST /api/actions/{proposal_id}/reject` -> `ActionProposalRecord` to `backend/app/routes/actions.py`, with the same guard order as the approve route so the two paths cannot diverge: `check_rbac(identity.role, "A7")` first (403), then the pool/503 guard, then the row `SELECT` (404), then the status guard raising HTTP 409 when `status != "PENDING_APPROVAL"`. Set `status = "REJECTED"`, `approved_by = identity.user_id`, `approved_at = now`, and `execution_result = "Rejected by <role>; this action was not executed."` composed server-side from the identity, never from a client-supplied string. Then `log_event(pool, {... "agent_id": "C3", "action_type": "ACTION_REJECTED", "approval_id": proposal_id, ...})`. Add a docstring line recording that `approved_by`/`approved_at` are reused for the reject decision deliberately — they record who decided and when, not who assented — so the schema carries one decision-provenance pair rather than two half-populated ones, and route the naming to SENT-7-05.

Add the blocked-attempt audit path to the `generate-capa` route: when `route_action(proposal["action_type"])` lands in `BLOCKED_CATEGORIES`, no row is written to `action_proposals` and `log_event` is called with `action_type="PROPOSAL_BLOCKED"` and an `output_summary` naming the category and `describe_category(category)`'s Bible text. Record in a comment that Bible Section 2 says only "Blocked immediately" for PROHIBITED and does not state that the attempt is logged; logging it is this project's reading of 21 CFR 11.10(e)'s tamper-evident-record intent (05-RESEARCH.md Assumption A5), and the deviation is routed to SENT-7-05.

Extend `backend/tests/test_routes_actions.py`:
- `test_reject_moves_proposal_to_rejected_and_audits`: generate, reject, assert HTTP 200, `status == "REJECTED"`, `approved_by` equals the request `X-User-Id`, and a new `audit_events` row exists with `action_type == "ACTION_REJECTED"`.
- `test_approve_after_reject_returns_409`, and its mirror `test_reject_after_approve_returns_409` — D-02's guarantee that a decision is terminal.
- `test_reject_denied_for_auditor_role` asserting HTTP 403 and an unchanged row status.
- `test_prohibited_proposal_is_never_queued`: call `persist_proposal`'s caller path with an `action_type` mapping to PROHIBITED (inject it through `c3_gateway.route_action`'s allowlist rather than by monkeypatching the function), assert `action_proposals` row count is unchanged and an `audit_events` row with `action_type == "PROPOSAL_BLOCKED"` exists.
- `test_approval_response_is_server_trusted`: introspect `ActionProposalRecord.model_fields` and assert the field-name set is exactly the expected literal set; then assert, for a real queued proposal, that `category` equals `route_action(row["action_type"])` and that every remaining field either equals the corresponding database column value or is `None`. Its docstring records REM-03: a field the server cannot trace to a column or to the frozen allowlist is a field a model could have authored.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_routes_actions.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && python -m pytest tests/test_routes_actions.py -x` exits 0.
    - Tests named `test_reject_moves_proposal_to_rejected_and_audits`, `test_approve_after_reject_returns_409`, `test_reject_after_approve_returns_409`, `test_reject_denied_for_auditor_role`, `test_prohibited_proposal_is_never_queued`, and `test_approval_response_is_server_trusted` all exist and pass.
    - `python -c "from app.main import app; print('/api/actions/{proposal_id}/reject' in {getattr(r,'path','') for r in app.routes})"` prints `True`.
    - A test asserts that after a PROHIBITED routing decision, `SELECT count(*) FROM action_proposals` is unchanged and an `audit_events` row with `action_type = 'PROPOSAL_BLOCKED'` exists.
    - `cd backend && python -m pytest` exits 0.
  </acceptance_criteria>
  <done>A human can reject a pending proposal; approve and reject are mutually terminal and both audit-logged; a PROHIBITED proposal is blocked and recorded rather than queued; and every approval-queue response field is provably traceable to a column or to the frozen category allowlist.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| A7 model output -> C3 | Model-authored `action_type` and narrative text cross into a routing decision |
| browser -> approve/reject routes | An untrusted caller attempts a terminal state transition on a queued write |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-21 | Elevation of Privilege | A model-authored `action_type` steering its own proposal into a permissive category | high | mitigate | `route_action` is a dict lookup over a frozen allowlist with a `PROHIBITED` default, so an invented `action_type` cannot land anywhere permissive; asserted by a parametrised fail-closed test and by `test_all_five_bible_categories_are_reachable`. |
| T-05-22 | Elevation of Privilege | An unverified finding reaching a queued write via A7 | high | mitigate | `A7_ELIGIBLE_CONFIDENCE` excludes `INSUFFICIENT_EVIDENCE` and every unrecognised grade; an AST gate proves A7 cannot re-run verification to manufacture its own eligibility. |
| T-05-23 | Tampering | SQL injection through a model-authored `action_type` | medium | mitigate | `test_persist_proposal_binds_every_value` stores a SQL metacharacter payload and asserts the exact literal round-trips with exactly one row inserted. |
| T-05-24 | Repudiation | A decision (approve or reject) with no attributable actor or no record | medium | mitigate | Both routes write `approved_by`/`approved_at` and emit `ACTION_APPROVED` / `ACTION_REJECTED` audit events carrying `user_id`, `user_role`, and `approval_id`. |
| T-05-25 | Elevation of Privilege | Re-deciding a proposal that already reached a terminal state | medium | mitigate | Both routes raise HTTP 409 unless `status == "PENDING_APPROVAL"`; asserted in both directions. |
| T-05-26 | Tampering | A model-authored value rendered by the approval dialog as though server-trusted | high | mitigate | `test_approval_response_is_server_trusted` pins the response field set and asserts every field traces to a database column or to `route_action`'s frozen allowlist; the CAPA structure and due date are computed in Python rather than parsed from model prose. |
| T-05-27 | Repudiation | A PROHIBITED attempt leaving no trace | low | mitigate | `PROPOSAL_BLOCKED` audit event, recorded as this project's reading of 21 CFR 11.10(e) (Assumption A5) and routed to SENT-7-05. |
| T-05-SC | Tampering | npm/pip/cargo installs | high | mitigate | No new package (`ast`, `datetime`, `json` are stdlib). |
</threat_model>

<verification>
1. `cd backend && python -m pytest tests/test_c3_gateway.py tests/test_a7_remediation.py tests/test_routes_actions.py -x` exits 0.
2. `cd backend && python -m pytest` exits 0.
3. `set(ACTION_CATEGORIES.values())` is exactly the five Bible categories.
</verification>

<success_criteria>
- All five Bible categories are reachable and asserted; unknown types fail closed to PROHIBITED.
- A7 produces nothing from an INSUFFICIENT_EVIDENCE finding and cannot import the verifier.
- Reject exists, is audit-logged, and is mutually terminal with approve.
- A PROHIBITED proposal is blocked and recorded rather than queued.
- Every approval-queue response field traces to a column or to the frozen allowlist.
</success_criteria>

<output>
Create `.planning/phases/05-safety-remediation/05-04-SUMMARY.md` when done, recording under "Deviations" the `PROPOSAL_BLOCKED` audit-logging choice (Assumption A5) and the `approved_by`/`approved_at` reuse for rejections, both routed to SENT-7-05.
</output>
