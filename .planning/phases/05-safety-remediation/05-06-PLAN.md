---
phase: 05-safety-remediation
plan: 06
type: execute
wave: 4
depends_on: ["05-02", "05-04", "05-05"]
files_modified:
  - backend/app/agents/c2_gateway.py
  - backend/app/agents/c3_gateway.py
  - backend/app/agents/a7_remediation.py
  - backend/app/agents/a0_orchestrator.py
  - backend/app/graph/state.py
  - backend/tests/test_graph_gateways.py
  - backend/tests/test_graph_topology.py
  - backend/tests/test_hero_loop.py
  - backend/tests/test_hero_tracer.py
autonomous: false
requirements: [SAFE-01, SAFE-02, REM-01, REM-02]
user_setup: []

estimate:
  tokens: 56000
  raw_tokens: 56000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Invoking the compiled graph with a jailbreak query blocks it at C2, and no specialist agent and no model call runs for that request."
    - "Invoking the compiled graph as Auditor cannot fan out to A3 through A6 even when A0 selects them."
    - "A7 does not synthesize inside the graph unless remediation was explicitly requested, honouring D-03."
    - "The eleven-node topology and its edge list are byte-identical to Phase 2's, proven by the existing topology assertions still passing unchanged."
    - "The full Monitor-to-Audit Phase 5 surface is walkable by a human in the browser end to end."
  artifacts:
    - backend/tests/test_graph_gateways.py
  key_links:
    - "graph/state.py safety_gateway_c2 -> c2_gateway.run_c2 (mirrors the shipped compliance_a2 -> run_a2 delegation)"
    - "run_c2 -> AgentState['permitted_agents'] -> route_specialists intersection (RBAC enforced without a topology change)"
    - "run_a0 -> early return when state['blocked'] (no model call on a blocked request)"
---

<objective>
Replace the last three stub nodes in the compiled LangGraph — C2, A7, C3 — with delegating calls to the real modules this phase built, so the Bible's fixed topology carries real deterministic gating rather than an admit-everything placeholder, and then verify the whole Phase 5 surface by hand in the browser.

Purpose: ROADMAP Phase 5's goal is that "every request is deterministically gated by RBAC and injection detection **before it reaches an agent**" — a claim about the orchestration path, not only about the HTTP routes. Leaving `safety_gateway_c2` returning `{"user_intent": "safe"}` while shipping "C2 is real" would be exactly the Bible drift CLAUDE.md Rule 14 and ticket SENT-7-05 exist to prevent.
Output: the three node adapters, the graph wiring, blocked-request short-circuiting, and the phase's human verification gate.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/05-safety-remediation/05-CONTEXT.md
@.planning/phases/05-safety-remediation/05-RESEARCH.md
@.planning/phases/05-safety-remediation/05-02-SUMMARY.md
@.planning/phases/05-safety-remediation/05-04-SUMMARY.md
@.planning/phases/05-safety-remediation/05-05-SUMMARY.md
@backend/app/graph/state.py
@backend/app/agents/a0_orchestrator.py
</context>

<artifacts>
## Artifacts this plan produces

**New file**
`backend/tests/test_graph_gateways.py`

**New symbols**
- `c2_gateway.py`: `run_c2`
- `c3_gateway.py`: `run_c3`
- `a7_remediation.py`: `run_a7`
- `a0_orchestrator.py`: `extract_user_query` (promoted from `_extract_user_query`)
- `graph/state.py`: `AgentState` fields `user_id`, `user_role`, `permitted_agents`, `blocked`, `blocked_reason`, `remediation_requested`
</artifacts>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Node adapters for C2, A7, and C3, and a blocked-request short circuit in A0</name>
  <files>backend/app/agents/c2_gateway.py, backend/app/agents/a7_remediation.py, backend/app/agents/c3_gateway.py, backend/app/agents/a0_orchestrator.py</files>
  <read_first>
    - `backend/app/agents/c1_verifier.py` `run_c1` (lines ~262-286) — the node-adapter shape every delegate in this codebase follows: takes `state`, returns a partial-state dict, never raises to the graph
    - `backend/app/agents/a0_orchestrator.py` (whole file) — `_extract_user_query`, `FULL_AGENT_SET`, `A0_TIMEOUT_SECONDS`, and `run_a0`'s never-raise contract
    - `backend/app/agents/c2_gateway.py`, `c3_gateway.py`, `a7_remediation.py` (as 05-02 and 05-04 left them)
    - `backend/app/graph/state.py` lines 1-64 and 150-250 — the module docstring's permanent no-model-in-C2/C1/C3 constraint and the three stub bodies being replaced
    - `.planning/phases/05-safety-remediation/05-CONTEXT.md` D-03 — A7 runs only on an explicit user action, never as a side effect of asking a question
    - `AegisX-AI-Project-Bible-v6.md` Section 2 C2 role line ("Enforces tool allowlists, RBAC, and prompt injection detection")
  </read_first>
  <behavior>
    - `run_c2` on a state carrying a jailbreak query returns `blocked=True` with a `blocked_reason` naming the detection leg, and `permitted_agents` empty.
    - `run_c2` on a benign query from `"Auditor"` returns `blocked=False` and `permitted_agents == ["A1", "A2"]`.
    - `run_c2` on a benign query from `"IT System Manager"` returns all seven agent ids.
    - `run_c2` on an unrecognised role returns `blocked=True` — fail closed.
    - `run_c2` on a state with no `user_role` key at all returns `blocked=True` — absent identity is not a permissive default.
    - `run_a0` returns `{"active_agents": []}` without calling the classifier when `state["blocked"]` is true.
    - `run_a7` returns `{"proposed_actions": []}` when `state` does not set `remediation_requested`, and synthesizes when it does and an eligible verified finding exists.
    - `run_c3` categorises every entry in `state["proposed_actions"]` and returns a `final_synthesis` string naming how many are queued and how many are blocked.
  </behavior>
  <action>
In `backend/app/agents/a0_orchestrator.py`: rename `_extract_user_query` to `extract_user_query` (public) and update its one call site in `run_a0`. Nothing else in the repository references the private name, so this is a straight rename — it exists so C2 and A0 read the user query through one function rather than two copies, the same single-source-of-truth discipline `RULE_EVIDENCE_TABLES` already sets. Then add a short-circuit as the first statement of `run_a0`: `if state.get("blocked"): return {"active_agents": [], "user_intent": "blocked"}`. Its comment records that this is what makes "blocked before it reaches an agent" literally true — A0 is the first node after C2 and the first place a model would otherwise be called, so returning here means a blocked payload never reaches any provider.

In `backend/app/agents/c2_gateway.py` add `async def run_c2(state: Dict[str, Any]) -> Dict[str, Any]` — the node adapter, following `run_c1`'s shape. It reads `role = state.get("user_role")` and `query = extract_user_query(state)` (imported from `app.agents.a0_orchestrator`), then:
- If `role` is None or `role not in PERMISSION_MATRIX`, return `{"blocked": True, "blocked_reason": f"rbac_unknown_role:{role}", "permitted_agents": [], "user_intent": "blocked"}`. Absent identity fails closed here, not open — record in the docstring that the graph is an in-process path and every HTTP write route already enforces `check_rbac` independently, so this is defence in depth rather than the only gate.
- Else if `detect_injection(query)` returns a reason, return `{"blocked": True, "blocked_reason": reason, "permitted_agents": [], "user_intent": "blocked"}`.
- Else return `{"blocked": False, "blocked_reason": None, "permitted_agents": sorted(PERMISSION_MATRIX[role]), "user_intent": "safe"}`.
Docstring states that this adapter performs no I/O and no model call, that it returns a partial-state dict and never raises to the graph, and that `permitted_agents` is how RBAC is enforced without touching the Bible's fixed topology: C2 publishes the caller's allowed set and the existing `route_specialists` fan-out intersects against it, which is a data change through an unchanged conditional edge exactly as `state.py`'s own docstring already claims for A0's subset routing.

In `backend/app/agents/a7_remediation.py` add `async def run_a7(state: Dict[str, Any]) -> Dict[str, Any]`. If `state.get("blocked")` is true or `state.get("remediation_requested")` is not true, return `{"proposed_actions": []}`. Otherwise iterate `state.get("findings", [])`, look each finding's verdict up in `state.get("verification_results", {})` by `finding_id`, call `synthesize_capa` for each, and return `{"proposed_actions": [p for p in results if p is not None]}`. The docstring records D-03 explicitly: A7 defaults to producing nothing inside the graph because remediation is an opt-in user action taken on a finding already on screen, never a side effect of asking a question — and that this is a real implementation of that decision, not a leftover stub. Note that the HTTP `generate-capa` route remains the only caller that sets `remediation_requested`.

In `backend/app/agents/c3_gateway.py` add `async def run_c3(state: Dict[str, Any]) -> Dict[str, Any]`. Categorise each entry of `state.get("proposed_actions", [])` via `route_action`, count how many fall in `QUEUED_CATEGORIES` and how many in `BLOCKED_CATEGORIES`, and return `{"final_synthesis": ...}` — a server-composed sentence naming the counts, or, when `state.get("blocked")` is true, a sentence naming `state.get("blocked_reason")`. This node performs no database write: persistence is `persist_proposal`'s job and it is called from the HTTP route, which is the only place an identity exists to attribute the row to. Record that separation in the docstring.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_graph_gateways.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && python -m pytest tests/test_c2_gateway.py tests/test_c3_gateway.py tests/test_a7_remediation.py tests/test_a0_orchestrator.py -x` exits 0.
    - `python -c "from app.agents.a0_orchestrator import extract_user_query; print(callable(extract_user_query))"` prints `True`.
    - `python -c "import asyncio;from app.agents.c2_gateway import run_c2;print(asyncio.run(run_c2({'messages':[],'user_role':'Auditor'}))['permitted_agents'])"` prints `['A1', 'A2']`.
    - `python -c "import asyncio;from app.agents.c2_gateway import run_c2;print(asyncio.run(run_c2({'messages':[],'system_id':'X'}))['blocked'])"` prints `True`.
    - `python -c "import asyncio;from app.agents.a7_remediation import run_a7;print(asyncio.run(run_a7({'findings':[{'finding_id':'F1'}],'verification_results':{'F1':{'confidence':'HIGH'}}})))"` prints `{'proposed_actions': []}`.
    - The AST no-model-dependency gates for `c2_gateway.py` and `c3_gateway.py` still pass after the adapters are added.
  </acceptance_criteria>
  <done>Three node adapters exist in the same shape as the shipped `run_c1`, C2 fails closed on both an unknown and an absent role, A0 makes no model call on a blocked request, and A7 produces nothing inside the graph unless remediation was explicitly requested.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the three stub nodes and prove the topology is unchanged</name>
  <files>backend/app/graph/state.py, backend/tests/test_graph_gateways.py, backend/tests/test_graph_topology.py, backend/tests/test_hero_loop.py, backend/tests/test_hero_tracer.py</files>
  <read_first>
    - `backend/app/graph/state.py` (whole file) — `AgentState`, the eleven node bodies, `route_specialists`, and the graph-assembly block that must stay byte-identical
    - `backend/tests/test_graph_topology.py` (whole file) — the node-set and edge-set assertions and its `_initial_state()` helper
    - `backend/tests/test_hero_loop.py` lines 60-200 — its `_initial_state()` helper and how it drives `compiled_graph.ainvoke`
    - `backend/tests/test_hero_tracer.py` lines 60-160 — same, plus its respx mocking setup
    - `backend/app/agents/c2_gateway.py`, `a7_remediation.py`, `c3_gateway.py` (as Task 1 left them)
  </read_first>
  <precondition>Postgres and OPA are running, since `test_hero_loop.py` and `test_hero_tracer.py` drive the graph against live services.</precondition>
  <action>
In `backend/app/graph/state.py`:
- Add six fields to `AgentState`: `user_id: str`, `user_role: str`, `permitted_agents: List[str]`, `blocked: bool`, `blocked_reason: Optional[str]`, `remediation_requested: bool`. None of them takes a reducer — each is last-writer-wins, which is correct because exactly one node writes each.
- Replace the three stub bodies with delegating one-liners in exactly the style `compliance_a2` and `evidence_verifier_c1` already use: `safety_gateway_c2` returns `await run_c2(state)`, `remediation_a7` returns `await run_a7(state)`, `action_gateway_c3` returns `await run_c3(state)`. Import the three at module top level alongside the existing agent imports. Each docstring is rewritten to describe the real behaviour and to keep the permanent constraint sentence: C2 and C3 are deterministic Python forever and A7 is the one node in this path permitted a model call, and only over already-verified data.
- Modify `route_specialists` to intersect: return a `Send` only for agents that are both in `state["active_agents"]` and in `state.get("permitted_agents", [])`. Its docstring gains a paragraph stating that this is still a data change through the same unchanged conditional edge — the function's node list, the graph's edges, and the assembly block below are untouched — and that the intersection is what stops an Auditor's query from fanning out to A3 through A6 even if A0 selects them.
- Update the module docstring: add a Phase 5 paragraph in the same style as the existing Phase 3 paragraphs, naming plan 05-06, the three delegates, and the fact that C2, C1, and C3 remain permanently closed to model occupancy.
- Do not edit the graph-assembly block (`add_node` / `add_edge` / `set_entry_point` / `compile`). It must stay byte-identical to Phase 2.

Add `user_id` and `user_role` to the `_initial_state()` helper in each of `backend/tests/test_graph_topology.py`, `backend/tests/test_hero_loop.py`, and `backend/tests/test_hero_tracer.py`, using `"IT System Manager"` so those existing suites keep exercising the full six-agent fan-out. Add a one-line comment in each pointing at plan 05-06 and explaining that C2 now fails closed on an absent role, so a graph invocation must carry identity. Change nothing else in those three files.

Create `backend/tests/test_graph_gateways.py` covering both the adapters from Task 1 and the wired graph:
- `test_jailbreak_query_is_blocked_at_c2_and_no_specialist_runs`: invoke `compiled_graph.ainvoke` with a state whose message is a Bible-literal jailbreak phrase, and assert the result has `blocked` true, a `blocked_reason` starting `regex_match:`, an empty `findings` list, and an empty `active_agents` list. Guard it with a `respx` route (or a monkeypatched `call_llm`) that raises if hit, so the test fails loudly if any model call escaped — this is the assertion that makes "blocked before it reaches an agent" mechanical rather than narrative.
- `test_auditor_role_cannot_fan_out_beyond_a1_a2`: invoke with `user_role="Auditor"`, force A0 to return the full six-agent set, and assert every finding in the result was produced by A1 or A2 only.
- `test_absent_role_is_blocked`: invoke with no `user_role` and assert `blocked` is true.
- `test_a7_does_not_synthesize_without_an_explicit_request`: invoke a normal query and assert `proposed_actions` is empty even when eligible verified findings exist, then invoke with `remediation_requested=True` and assert it is non-empty.
- `test_topology_is_unchanged_after_wiring`: assert the graph builder's node set and edge set match the same literals `test_graph_topology.py` asserts, so a future edit to a node body cannot silently reshape the graph.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_graph_gateways.py tests/test_graph_topology.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && python -m pytest` exits 0.
    - Tests named `test_jailbreak_query_is_blocked_at_c2_and_no_specialist_runs`, `test_auditor_role_cannot_fan_out_beyond_a1_a2`, `test_absent_role_is_blocked`, `test_a7_does_not_synthesize_without_an_explicit_request`, and `test_topology_is_unchanged_after_wiring` all exist and pass.
    - Every pre-existing test in `backend/tests/test_graph_topology.py` still passes with its assertions unedited.
    - `python -c "from app.graph.state import graph; print(sorted(graph.nodes))"` prints exactly `['A0', 'A1', 'A2', 'A3', 'A4', 'A5', 'A6', 'A7', 'C1', 'C2', 'C3']`.
    - `python -c "from app.graph.state import graph; print(len(graph.edges))"` prints the same edge count `test_graph_topology.py` asserted before this plan, and `test_topology_is_unchanged_after_wiring` passes — these two together are the structural proof that the assembly block was not reshaped, replacing any text-diff heuristic.
  </acceptance_criteria>
  <done>C2, A7, and C3 delegate to their real modules, an Auditor's query provably cannot fan out past A1/A2, a jailbreak query is blocked before any model call, and the eleven-node topology and edge list are unchanged.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification of the Phase 5 demo surface</name>
  <read_first>
    - `.planning/ROADMAP.md` Phase 5 "Success Criteria" — the five statements this checkpoint is confirming
    - `.planning/phases/05-safety-remediation/05-UI-SPEC.md` — the Copywriting Contract and the five-status colour table the screens should match
    - `.planning/phases/05-safety-remediation/05-05-SUMMARY.md` — what the Approval Centre actually shipped, including any recorded UI-contract deviation
  </read_first>
  <what-built>
    The full Phase 5 surface: C2 RBAC and deterministic injection detection (wired into both the HTTP write routes and the compiled graph's C2 node), A7 CAPA synthesis from C1-verified findings only, C3 five-category action routing with GxP-relevant writes parked PENDING_APPROVAL, the hash-chained audit trail with `verify_chain()` and the tamper demo endpoint, live proposal push over the existing `/api/copilot/stream/{session_id}` socket, and the Action / Approval Centre with a persistent role selector.

    Before presenting this checklist, bring the stack up rather than asking the developer to: `docker compose up -d postgres qdrant opa`, then `bash infra/apply-migrations.sh`, then start the backend on port 8000 and `npm run dev` on port 3000 in the background, and confirm `curl -s http://127.0.0.1:8000/api/health` returns 200 and port 3000 answers.
  </what-built>
  <how-to-verify>
    Walk these nine items in a real browser at http://localhost:3000 and report the result of each:

    1. The role selector is visible in the app chrome on every page and shows `IT System Manager`, `QA/Compliance`, `Auditor`.
    2. As `Auditor`, open `/findings` and click Generate CAPA — the permission sentence from the UI contract appears inline, and no proposal is created.
    3. Switch to `IT System Manager` and click Generate CAPA on the same finding — a confirmation naming a proposal id appears.
    4. With `/actions` open in a second tab during step 3, the new proposal appears there with no manual refresh and the live indicator shows connected.
    5. On `/actions`, the card shows ACTION TYPE, CATEGORY, TARGET SYSTEM, JUSTIFICATION, PAYLOAD and a `PENDING_APPROVAL` badge; a large payload scrolls inside its box rather than stretching the page.
    6. Click Reject on one proposal — the confirmation names that proposal's own action type and target system, and after confirming, the badge reads `REJECTED`.
    7. Click Approve on another — the button reads `Approving...` while in flight and the badge lands on `APPROVED` or `EXECUTED`.
    8. `curl -s http://127.0.0.1:8000/api/audit/verify` reports `VERIFIED`; then run the tamper demo against one of the event ids just created and confirm it reports `TAMPERED` with a `broken_at_index`.
    9. Nothing on `/`, `/copilot`, `/blast-radius`, or `/findings` regressed visually from Phase 4.

    Record the response verbatim in `05-06-SUMMARY.md` under a "Human verification" heading. If any item fails, capture what was observed and raise it as a gap rather than marking the phase done.
  </how-to-verify>
  <acceptance_criteria>
    - The developer has responded to all nine checklist items.
    - Their response is recorded verbatim in `05-06-SUMMARY.md` under a "Human verification" heading.
    - Any failed item is listed as a gap with what was observed, not silently omitted.
  </acceptance_criteria>
  <resume-signal>Type "approved" or describe which of the nine items failed and what you saw</resume-signal>
  <done>A human has walked the Phase 5 surface end to end and the result — pass or gap list — is recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| graph state -> C2 node | The in-process path where a user query and an asserted role enter orchestration |
| C2 node -> A0 and the specialist fan-out | The point at which a payload would otherwise reach a model provider |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-35 | Elevation of Privilege | A blocked payload still reaching a model because the topology has no C2-to-END edge | high | mitigate | `run_a0` returns immediately on `state["blocked"]`, before `classify_intent`, and `route_specialists` emits no `Send` for an empty `permitted_agents`. Asserted by a test that fails if any model call is attempted during a blocked invocation. |
| T-05-36 | Elevation of Privilege | An under-privileged role's query fanning out to agents its role forbids | high | mitigate | `run_c2` publishes `permitted_agents` and `route_specialists` intersects `active_agents` against it, so A0's selection can only narrow the permitted set, never widen it. Asserted by `test_auditor_role_cannot_fan_out_beyond_a1_a2`. |
| T-05-37 | Spoofing | A graph invocation with no identity defaulting to a permissive role | high | mitigate | `run_c2` treats both an absent and an unrecognised role as blocked; the three existing graph test helpers were updated to pass identity rather than relaxing the gate. |
| T-05-38 | Tampering | A future edit reshaping the Bible's fixed topology while wiring node bodies | medium | mitigate | The assembly block is left byte-identical, `test_topology_is_unchanged_after_wiring` asserts the node and edge sets, and an acceptance gate checks the diff introduced no `add_edge` change. |
| T-05-39 | Elevation of Privilege | A7 firing automatically and queueing writes nobody asked for | medium | mitigate | `run_a7` returns an empty proposal list unless `remediation_requested` is set, which only the HTTP `generate-capa` route sets (D-03); asserted in both directions. |
| T-05-40 | Repudiation | A block decision inside the graph leaving no record | low | accept | The graph path has no identity-bearing HTTP context to attribute an audit row to, and Phase 5's audit-logged surface is the write-capable routes (D-04). `blocked_reason` is returned in the result state so the caller can log it; wiring graph-level audit events belongs with Phase 6's `/api/copilot/query`, where a session and identity exist. |
| T-05-SC | Tampering | npm/pip/cargo installs | high | mitigate | No new package — `langgraph`, `langchain-core`, `respx`, and `pytest` are already pinned. |
</threat_model>

<verification>
1. `cd backend && python -m pytest` exits 0, including the three pre-existing graph suites with their assertions unedited.
2. `python -c "from app.graph.state import graph; print(len(graph.nodes))"` prints `11`.
3. `test_topology_is_unchanged_after_wiring` passes and every pre-existing assertion in `backend/tests/test_graph_topology.py` passes unedited, proving the assembly block was not reshaped.
4. The human verification checklist is answered and recorded.
</verification>

<success_criteria>
- `safety_gateway_c2`, `remediation_a7`, and `action_gateway_c3` delegate to real modules, matching the shipped `compliance_a2` / `evidence_verifier_c1` pattern.
- A jailbreak query is blocked at C2 with no model call anywhere downstream.
- An Auditor's query cannot fan out beyond A1 and A2.
- A7 stays silent inside the graph unless remediation was explicitly requested.
- The eleven-node topology and edge list are unchanged.
- A human has walked the Phase 5 surface and the outcome is recorded.
</success_criteria>

<output>
Create `.planning/phases/05-safety-remediation/05-06-SUMMARY.md` when done, with a "Human verification" heading carrying the developer's verbatim response and a "Bible reconciliation" section listing every Phase 5 deviation collected across plans 05-01 through 05-06 for ticket SENT-7-05.
</output>
