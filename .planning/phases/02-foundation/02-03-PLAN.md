---
phase: 02-foundation
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/requirements.txt
  - backend/pytest.ini
  - backend/app/__init__.py
  - backend/app/main.py
  - backend/app/schemas.py
  - backend/tests/__init__.py
  - backend/tests/conftest.py
  - backend/tests/test_health.py
  - backend/tests/test_schemas.py
  - backend/README.md
autonomous: true
requirements: [ENV-04]
tickets: [SENT-1-05]

estimate:
  tokens: 52000
  raw_tokens: 26000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A project-local `backend/.venv` exists with every Stage 1 backend dependency installed at a pinned version, and nothing was installed into the machine's global Anaconda environment (ENV-04)."
    - "`GET /api/health` returns HTTP 200 with body exactly `{\"status\": \"ok\"}`, both through the test client and against a live uvicorn process (ENV-04)."
    - "Every Pydantic model in Bible Section 4.3 imports from `app.schemas` and validates a well-formed instance, and rejects a malformed one (ENV-04)."
    - "`pytest` runs from `backend/` and is the command every later backend plan and CI extends rather than reinvents."
  artifacts:
    - backend/requirements.txt
    - backend/app/main.py
    - backend/app/schemas.py
    - backend/pytest.ini
    - backend/tests/conftest.py
  key_links:
    - "`backend/app/main.py` FastAPI instance → the object plan 02-07 attaches the `/api/copilot/stream/{session_id}` WebSocket route to"
    - "`backend/requirements.txt` → the single dependency list plans 02-05, 02-06, 02-07 and CI all install from, so no later plan edits it"
    - "`backend/tests/conftest.py` fixtures → reused by `test_opa_client.py` (02-05), `test_graph_topology.py` (02-06), `test_ws_echo.py` (02-07)"
---

<objective>
Stand up the FastAPI application tier: a project-local virtual environment with pinned dependencies, every Bible Section 4.3 Pydantic model importable and validating, a live `/api/health` returning 200, and a pytest harness the rest of the phase builds on.

This plan covers Build-Map ticket **SENT-1-05** (FastAPI skeleton + Pydantic schemas). Per BRANCHING.md §4 it owns "the FastAPI application entrypoint and the Pydantic schema modules". It also establishes the backend's shared scaffolding — dependency list, test configuration, and fixtures — deliberately and completely in wave 1, so that the three wave-2 backend plans (02-05 OPA client, 02-06 StateGraph, 02-07 WebSocket) each add only their own new files and never contend over `requirements.txt` or `pytest.ini`. That is Rule 10 enforced by allocation rather than by hope.

Purpose: `/api/health` returning 200 is one of the four literal clauses of the Stage 1 gate, and the Section 4.3 types are the contract every agent in Phase 3 emits against.
Output: A runnable, tested FastAPI skeleton.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-foundation/02-RESEARCH.md
@CLAUDE.md
@BRANCHING.md
@backend/README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Project-local virtualenv and the pinned Stage 1 dependency set</name>
  <files>backend/requirements.txt, backend/pytest.ini, backend/app/__init__.py, backend/tests/__init__.py, backend/README.md</files>
  <read_first>
    - `.planning/phases/02-foundation/02-RESEARCH.md` lines 63-116 — the verified backend package versions and the installation commands, including the explicit warning not to install into the global environment.
    - `.planning/phases/02-foundation/02-RESEARCH.md` lines 117-137 — the Package Legitimacy Audit. Every backend package this task installs already carries an `OK` verdict there, which is why this plan needs no install checkpoint.
    - `.planning/phases/02-foundation/02-RESEARCH.md` lines 872-876 — Pitfall 5, the Anaconda global-environment hazard on this machine.
    - `.planning/phases/02-foundation/02-RESEARCH.md` lines 884-896 — Environment Availability: `python` (not `python3`) resolves here, at 3.13.9.
    - `.gitignore` — confirms `.venv/` and `__pycache__/` are already ignored, so the environment must not appear in `git status`.
    - `backend/README.md` — the Phase 1 tier README to extend.
  </read_first>
  <action>
Create `backend/requirements.txt` listing exactly the Stage 1 backend dependency set, each pinned to an exact `==` version:

`fastapi`, `pydantic`, `uvicorn[standard]`, `httpx`, `langgraph`, `langchain-core`, `pytest`.

Use the versions verified in 02-RESEARCH.md lines 63-76 as the starting point: fastapi 0.141.1, pydantic 2.13.4, uvicorn 0.52.4, httpx 0.28.1, langgraph 1.2.11, langchain-core 1.6.0. The research did not pin a `pytest` version — resolve it at execution time with `pip index versions pytest` and pin whatever that returns rather than writing a remembered number. Before committing, confirm every pin actually resolves during install; if any has moved, pin the version that installed and note the change in the SUMMARY.

Install all seven now, in wave 1, even though `langgraph` and `langchain-core` are not used until plan 02-06 and `httpx` not until 02-05. Splitting installs across waves would make `requirements.txt` a file three wave-2 plans all need to edit, which is exactly the simultaneous-edit collision BRANCHING.md §4 exists to prevent.

Add `pytest-asyncio` to nothing. It is absent from the Package Legitimacy Audit, and it is unnecessary: the one async assertion this phase needs (plan 02-06's `compiled_graph.ainvoke`) runs under `asyncio.run()` inside an ordinary synchronous test. Do not introduce any backend package that is not in the audit table.

Create the environment with `python -m venv backend/.venv` — note `python`, not `python3`, which is what resolves on this machine. Install with `backend/.venv/Scripts/python -m pip install -r backend/requirements.txt` (Windows layout; the Git Bash path has no `.exe` suffix). Never invoke the bare `pip` on PATH: it resolves to pip 25.3 inside `C:\Anaconda3`, a global environment shared with unrelated projects.

Create `backend/pytest.ini` with `[pytest]`, `testpaths = tests`, and `pythonpath = .`, so `app` is importable when pytest runs with `backend/` as rootdir. Create empty `backend/app/__init__.py` and `backend/tests/__init__.py`.

Extend `backend/README.md` with a `## Local setup (Stage 1)` section covering: the venv creation and install commands verbatim, the reason a project-local venv is mandatory here, the run command `backend/.venv/Scripts/python -m uvicorn app.main:app --reload --port 8000` executed from inside `backend/`, the test command `backend/.venv/Scripts/python -m pytest -x` executed from inside `backend/`, and the fact that the backend runs host-side this phase rather than as a Compose service (02-RESEARCH.md Open Question 1) so that `docker-compose.yml` needs no change and no BRANCHING.md §5 shared-file PR is triggered.
  </action>
  <verify>
    <automated>backend/.venv/Scripts/python -m pip check && backend/.venv/Scripts/python -c "import fastapi, pydantic, uvicorn, httpx, langgraph, langchain_core, pytest; print('imports ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `backend/.venv/Scripts/python -m pip check` exits 0 with no broken-dependency output.
    - `backend/.venv/Scripts/python -c "import fastapi, pydantic, uvicorn, httpx, langgraph, langchain_core, pytest"` exits 0.
    - `backend/.venv/Scripts/python -c "import sys; print(sys.prefix)"` prints a path under the repo's `backend/.venv`, not a path containing `Anaconda`.
    - `grep -c '==' backend/requirements.txt` outputs `7`, and `grep -cE '^[a-zA-Z]' backend/requirements.txt` also outputs `7` — every line is a pinned requirement, none is unpinned.
    - `grep -c 'pytest-asyncio' backend/requirements.txt` outputs `0`.
    - `git status --porcelain backend/` lists no `.venv` or `__pycache__` entry.
    - `backend/pytest.ini` contains both `testpaths = tests` and `pythonpath = .`.
    - `backend/README.md` contains the string `uvicorn app.main:app` and the string `.venv`.
  </acceptance_criteria>
  <done>A project-local, pinned, import-clean backend environment exists and is documented, and no later plan needs to touch the dependency list.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Transcribe the Bible Section 4.3 Pydantic models</name>
  <files>backend/app/schemas.py, backend/tests/test_schemas.py</files>
  <read_first>
    - `AegisX-AI-Project-Bible-v6.md` Section 4.3 (lines 834-937) — the authoritative model definitions. Source of truth (CLAUDE.md Rule 14).
    - `.planning/phases/02-foundation/02-RESEARCH.md` lines 518-653 — the same models transcribed and cross-checked, plus the recommended module layout.
    - `.planning/phases/02-foundation/02-RESEARCH.md` lines 878-882 — Pitfall 6, the deliberate coexistence of a Pydantic `AgentFinding` and a TypedDict `AgentFinding`.
    - `backend/pytest.ini` and `backend/tests/__init__.py` from Task 1.
  </read_first>
  <behavior>
    - Every one of the 12 Section 4.3 classes imports from `app.schemas`.
    - A fully-populated `AgentFinding` constructed with a valid `ALCOAScore` validates and round-trips through `model_dump()`.
    - An `AgentFinding` missing the required `claim` field raises `pydantic.ValidationError` rather than constructing a half-formed object.
    - An `OPAViolation` constructed from the exact dictionary shape `data.sentinel.gxp.violation` emits — `rule_id`, `severity`, `system_id`, `record_id`, `description` — validates without adaptation.
    - `ALCOAScore()` with no arguments applies the Bible's per-field defaults, five of which are `False`.
    - `AgentMessage()` populates `timestamp` automatically without the caller supplying one.
  </behavior>
  <action>
Create `backend/app/schemas.py` containing all 12 Bible Section 4.3 models, in the Bible's own order: `EvidenceRef`, `ALCOAScore`, `AgentFinding`, `ActionProposal`, `AgentMessage`, `AuditEvent`, `OPAViolation`, `ConfidenceAssessment`, `CAPAProposal`, `AuditPackage`, `SystemReadinessScore`, `AgentExecutionTrace`.

Transcribe field names, types, optionality, and defaults exactly. Specific points that are easy to smooth over and must not be:
- `ALCOAScore` has nine boolean fields whose defaults are not uniform: `attributable`, `contemporaneous`, and `original` default to `False`; `legible`, `accurate`, `complete`, `consistent`, `enduring`, and `available` default to `True`. Do not normalise them.
- `AgentFinding.confidence_score` is a `str`, not an enum, and its documented value domain is HIGH / MEDIUM / LOW / INSUFFICIENT_EVIDENCE. Keep it `str` and record the domain in a comment; introducing an enum here would change the type Phase 3's C1 verifier emits against.
- `EvidenceRef.reference_type` carries the Bible's `Field(..., description=...)` text verbatim.
- `AuditEvent` has 18 fields; `target_record_id`, `model_id`, and `approval_id` are `Optional[str]` and the rest are required.
- `AuditPackage.findings` is `List[AgentFinding]`, so the models are genuinely nested and the import order matters.

One mechanical deviation: the Bible writes `Field(default_factory=datetime.utcnow)` on `AgentMessage.timestamp`. `datetime.utcnow()` is deprecated from Python 3.12 and this machine runs 3.13.9, so it emits a `DeprecationWarning` on every construction. Replace it with a factory returning `datetime.now(timezone.utc).replace(tzinfo=None)`, which produces a byte-identical naive-UTC value with no warning. Keeping the result naive matters: switching to a timezone-aware datetime would make comparisons against the Bible's other naive `datetime` fields raise `TypeError`. Note the substitution in a comment on that line.

Head the module with a docstring naming `SENT-1-05`, `ENV-04`, and `AegisX-AI-Project-Bible-v6.md Section 4.3`, and stating plainly that these are the **application-layer** models used for validation at the FastAPI boundary, distinct from the **graph-state** `TypedDict` definitions plan 02-06 creates in `app/graph/state.py`. Both carry the names `AgentFinding` and `ActionProposal` with the same field names and are deliberately not the same Python type: LangGraph state uses `TypedDict` for zero per-step validation overhead, FastAPI uses `BaseModel` for runtime validation at the edge. State in the docstring that conversion between them happens explicitly at the API boundary and that neither module imports the other.

Create `backend/tests/test_schemas.py` covering the `<behavior>` list above. Assert the `ALCOAScore` default pattern field by field rather than comparing a whole dict, so a failure names the field that drifted. Assert the `AgentFinding` failure path with `pytest.raises(ValidationError)` — a construction test alone proves the class exists, not that it validates.
  </action>
  <verify>
    <automated>cd backend && .venv/Scripts/python -m pytest tests/test_schemas.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && .venv/Scripts/python -m pytest tests/test_schemas.py -x -q` exits 0.
    - `cd backend && .venv/Scripts/python -c "from app.schemas import EvidenceRef, ALCOAScore, AgentFinding, ActionProposal, AgentMessage, AuditEvent, OPAViolation, ConfidenceAssessment, CAPAProposal, AuditPackage, SystemReadinessScore, AgentExecutionTrace; print('12 ok')"` exits 0 and prints `12 ok`.
    - `grep -c '^class ' backend/app/schemas.py` outputs `12`.
    - `cd backend && .venv/Scripts/python -c "from app.schemas import ALCOAScore; s=ALCOAScore(); assert (s.attributable, s.contemporaneous, s.original)==(False,False,False); assert (s.legible,s.accurate,s.complete,s.consistent,s.enduring,s.available)==(True,)*6"` exits 0.
    - `cd backend && .venv/Scripts/python -W error::DeprecationWarning -c "from app.schemas import AgentMessage; m=AgentMessage(agent_id='A2', message='x'); assert m.timestamp.tzinfo is None"` exits 0 — no deprecation warning is raised and the timestamp is naive.
    - A test asserting `AgentFinding(**{...})` without `claim` raises `ValidationError` is present and passing.
    - `backend/app/schemas.py`'s module docstring names both `app/graph/state.py` and the reason the two `AgentFinding` definitions coexist.
  </acceptance_criteria>
  <done>All 12 Section 4.3 models import, validate a good instance, reject a bad one, and their relationship to the forthcoming graph-state types is written down where the next developer will read it.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: FastAPI entrypoint with a live /api/health</name>
  <files>backend/app/main.py, backend/tests/conftest.py, backend/tests/test_health.py, backend/README.md</files>
  <read_first>
    - `AegisX-AI-Project-Bible-v6.md` Section 12 (lines 1404-1420) — the API table. `GET /api/health` is specified to return `{"status": "ok"}`; the other ten endpoints in that table belong to later phases.
    - `.planning/phases/02-foundation/02-RESEARCH.md` lines 518-533 and 637-653 — the health endpoint's literal contract and the recommended `backend/app/` layout.
    - `backend/app/schemas.py` from Task 2 and `backend/pytest.ini` from Task 1.
    - `infra/health-check.sh` — the repo's existing health gate, and the `node -e "fetch(...)"` probe convention used instead of curl.
  </read_first>
  <behavior>
    - `GET /api/health` returns status 200 and a JSON body that equals `{"status": "ok"}` exactly — not a superset, not a different key.
    - `GET /api/health` is reachable on a live uvicorn process bound to port 8000, not only through the in-process test client.
    - `GET /` returns 404, confirming no unintended catch-all route was registered.
    - The application object imports without a database, an OPA server, or any network call.
  </behavior>
  <action>
Create `backend/app/main.py` with a module-level `app = FastAPI(...)`, titled for AegisX AI and carrying a short description. Register exactly one route: an async `GET /api/health` returning the literal dict `{"status": "ok"}`.

Register nothing else. Bible Section 12 lists ten further endpoints, and it is tempting to stub them so the OpenAPI page shows the eventual surface — do not. Those paths belong to tickets in Stages 2 through 5, and pre-registering them here would put this plan's branch inside other tickets' contracts, which CLAUDE.md Rule 7 forbids and BRANCHING.md §4 allocates against. The one exception arriving later in this phase is plan 02-07, which attaches the `/api/copilot/stream/{session_id}` WebSocket route to this same `app` object in wave 2 — sequentially, after this plan has merged, so the two never edit the file at once.

Import nothing from `app.graph` or `app.opa_client` here. Those modules do not exist until wave 2, and importing them now would make the application unimportable and every test in this plan fail.

Create `backend/tests/conftest.py` with the shared fixtures later backend plans will reuse rather than redefine:
- a session-scoped `client` fixture returning `fastapi.testclient.TestClient(app)`;
- an `opa_base_url` fixture returning `os.getenv("OPA_URL", "http://127.0.0.1:8181")`, which plan 02-05 consumes;
- a `pinned_now_ns` fixture returning a fixed nanosecond-epoch integer, so any backend test needing a reference clock uses the same constant the Rego suite pins to.
Keep the file to fixtures only; no assertions, no import of modules that do not yet exist.

Create `backend/tests/test_health.py` covering the `<behavior>` list, asserting on the exact response body and on the 404 for `/`.

Finally, extend `backend/README.md`'s Stage 1 section with the `/api/health` contract and the live-check command using `node -e "fetch('http://127.0.0.1:8000/api/health')"` rather than curl, matching the convention `infra/health-check.sh` established for this machine.
  </action>
  <verify>
    <automated>cd backend && .venv/Scripts/python -m pytest -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && .venv/Scripts/python -m pytest -x -q` exits 0 with every test in `tests/` passing.
    - With `backend/.venv/Scripts/python -m uvicorn app.main:app --port 8000` running from `backend/`, this exits 0: `node -e "fetch('http://127.0.0.1:8000/api/health').then(r=>r.json().then(j=>process.exit(r.status===200&&JSON.stringify(j)==='{\"status\":\"ok\"}'?0:1)))"`.
    - `cd backend && .venv/Scripts/python -c "from app.main import app; print(sorted(r.path for r in app.routes if getattr(r,'path','').startswith('/api')))"` prints exactly `['/api/health']`.
    - `cd backend && .venv/Scripts/python -c "from app.main import app"` succeeds with the Postgres and OPA containers stopped — the module has no import-time network or database dependency.
    - `backend/tests/conftest.py` defines all three fixtures `client`, `opa_base_url`, and `pinned_now_ns`.
    - A test asserting `GET /` returns 404 is present and passing.
  </acceptance_criteria>
  <done>`/api/health` returns 200 with the Bible's exact body, both in-process and against a live server, and the shared test fixtures are in place for waves 2 and 3.</done>
</task>

</tasks>

<artifacts_produced>
| Artifact | Kind | Notes |
|---|---|---|
| `backend/requirements.txt` | pins | 7 exactly-pinned packages: fastapi, pydantic, uvicorn[standard], httpx, langgraph, langchain-core, pytest. Owned solely by this plan; no later plan edits it. |
| `backend/.venv/` | env (gitignored) | Project-local interpreter, deliberately not the global Anaconda environment. |
| `backend/app/schemas.py` | Python | `EvidenceRef`, `ALCOAScore`, `AgentFinding`, `ActionProposal`, `AgentMessage`, `AuditEvent`, `OPAViolation`, `ConfidenceAssessment`, `CAPAProposal`, `AuditPackage`, `SystemReadinessScore`, `AgentExecutionTrace` — Pydantic `BaseModel` versions. |
| `backend/app/main.py` | Python | `app: FastAPI`; route `GET /api/health` → `{"status": "ok"}`. |
| `backend/pytest.ini` | config | `testpaths = tests`, `pythonpath = .`. |
| `backend/tests/conftest.py` | Python | Fixtures `client`, `opa_base_url`, `pinned_now_ns`. |
| `backend/tests/test_schemas.py`, `backend/tests/test_health.py` | tests | ENV-04 coverage. |
| `backend/README.md` | doc | Extended with `## Local setup (Stage 1)`. |

**Consumed downstream:** plan 02-05 (`opa_base_url` fixture, `OPAViolation`), plan 02-06 (`langgraph`/`langchain-core` pins), plan 02-07 (attaches the WebSocket route to `app`), plan 02-08 (CI installs `requirements.txt` and runs `pytest`), Phase 3 (all agents emit `AgentFinding`).
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PyPI → `backend/.venv` | Third-party code executes with the developer's privileges at install and at import. |
| HTTP client → FastAPI route handlers | Untrusted request data crosses into the application. In this phase the only route takes no input. |
| global Anaconda environment → project | A shared interpreter on this machine can silently supply a different version of a package than the project pinned. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-SC | Tampering | `pip install` supply chain | high | mitigate | All seven packages carry an `OK` verdict in the Package Legitimacy Audit (02-RESEARCH.md lines 117-137), each traced to a named source repository and cross-checked against the project's own CLAUDE.md or the Bible. No `[ASSUMED]` or `[SUS]` package is installed by this plan, so no blocking legitimacy checkpoint is required. Every version is `==` pinned, and `pip check` gates the result. Adding `pytest-asyncio` is explicitly ruled out because it is not in the audit table. |
| T-02-11 | Tampering | version drift via the global Anaconda environment | medium | mitigate | A project-local `backend/.venv` is mandatory, the bare `pip` on PATH is forbidden by the task action, and the acceptance criteria assert `sys.prefix` resolves inside the repo rather than under `Anaconda`. |
| T-02-12 | Information Disclosure | FastAPI debug surface | low | accept | Only `/api/health` is registered and it takes no input and returns a constant. The interactive OpenAPI docs expose nothing beyond that single route. The service binds to localhost during development. |
| T-02-13 | Spoofing | unauthenticated API surface | low | accept | ASVS V2/V4 are deliberately deferred to Phase 5 (C2 RBAC, SENT-4-01), per 02-RESEARCH.md "Security Domain". `/api/health` is an unauthenticated liveness probe by design and carries no user data. |
| T-02-14 | Tampering | unvalidated input reaching handlers | medium | mitigate | ASVS V5 is the one category that applies this phase, and its standard control is already the contract: every route boundary validates through the Section 4.3 Pydantic models. No route in this plan accepts a body, and Task 2's failure-path test proves the models actually reject malformed data rather than coercing it. |
</threat_model>

<verification>
Run from the repo root.

1. `backend/.venv/Scripts/python -m pip check` — exits 0.
2. `cd backend && .venv/Scripts/python -m pytest -x -q` — all tests pass.
3. Start the server: `cd backend && .venv/Scripts/python -m uvicorn app.main:app --port 8000`.
4. `node -e "fetch('http://127.0.0.1:8000/api/health').then(r=>r.json().then(j=>{console.log(r.status, JSON.stringify(j))}))"` — prints `200 {"status":"ok"}`.
5. Import-isolation check: stop the Compose stack entirely, then run `cd backend && .venv/Scripts/python -c "from app.main import app"` — must still succeed.
6. `git status --porcelain` — no `.venv/`, no `__pycache__/`, no `.pytest_cache/` appears.
</verification>

<success_criteria>
- All 12 Bible Section 4.3 Pydantic models are importable from `app.schemas` and validate correctly in both directions (ENV-04).
- `/api/health` returns 200 with body `{"status": "ok"}` against a live uvicorn process (ENV-04).
- Backend dependencies are installed into `backend/.venv` at pinned versions, all drawn from the audited package set, with `pip check` clean.
- `pytest` runs green from `backend/` and the shared fixtures exist for the wave-2 plans.
- No file outside this plan's declared `files_modified` was touched; `docker-compose.yml`, `.env.example`, root `README.md`, and `BRANCHING.md` are unmodified (BRANCHING.md §5).
</success_criteria>

<output>
Create `.planning/phases/02-foundation/02-03-SUMMARY.md` when done.
</output>
