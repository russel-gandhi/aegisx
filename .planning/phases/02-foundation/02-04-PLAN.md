---
phase: 02-foundation
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/package-lock.json
  - frontend/index.html
  - frontend/vite.config.ts
  - frontend/tsconfig.json
  - frontend/tsconfig.app.json
  - frontend/tsconfig.node.json
  - frontend/src/main.tsx
  - frontend/src/App.tsx
  - frontend/src/index.css
  - frontend/src/routes.tsx
  - frontend/src/components/PrototypeBanner.tsx
  - frontend/src/components/NavBar.tsx
  - frontend/src/components/AgentTopologyCanvas.tsx
  - frontend/src/pages/CommandCentre.tsx
  - frontend/src/pages/Copilot.tsx
  - frontend/src/pages/AuditReadiness.tsx
  - frontend/src/pages/Suppliers.tsx
  - frontend/src/pages/Actions.tsx
  - frontend/src/pages/AssuranceLab.tsx
  - frontend/src/pages/TrustCentre.tsx
  - frontend/src/pages/InspectionSimulator.tsx
  - frontend/src/__tests__/routes.test.tsx
  - frontend/vitest.setup.ts
  - frontend/README.md
autonomous: false
requirements: [UI-01]
tickets: [SENT-1-07]

estimate:
  tokens: 68000
  raw_tokens: 34000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`npm run build` completes with zero TypeScript errors and zero Vite errors, producing a `dist/` bundle (UI-01)."
    - "`npm run dev` serves the app on port 3000, and every one of the 8 documented Section 11 routes renders its own page rather than a shared placeholder or a router 404 (UI-01)."
    - "A React Flow canvas is mounted with placeholder nodes and edges and renders without the missing-stylesheet layout collapse that hits v12 when its CSS is not imported (UI-01)."
    - "Tailwind utility classes actually take visual effect, proving the v4 plugin path is wired rather than silently inert."
    - "The persistent prototype banner appears on every route."
    - "No npm package outside the audited set was installed without an explicit human legitimacy check first."
  artifacts:
    - frontend/package.json
    - frontend/vite.config.ts
    - frontend/src/routes.tsx
    - frontend/src/components/AgentTopologyCanvas.tsx
    - frontend/src/__tests__/routes.test.tsx
  key_links:
    - "`@tailwindcss/vite` plugin registered in vite.config.ts + `@import \"tailwindcss\";` in src/index.css → utility classes render"
    - "`frontend/src/routes.tsx` route table → the 8 Bible Section 11 pages, with room for a 9th (Blast Radius) in Phase 4"
    - "`frontend/src/pages/Copilot.tsx` → the page plan 02-07 attaches the WebSocket client to in wave 2"
---

<objective>
Deliver the frontend application shell: a Vite + React + TypeScript + Tailwind v4 app that boots on port 3000, routes to every page documented in Bible Section 11, mounts a React Flow canvas with placeholder nodes, and carries the mandatory prototype banner — with an automated build and route-render suite proving it.

This plan covers Build-Map ticket **SENT-1-07** (React/Vite/Tailwind shell). Per BRANCHING.md §4 it owns `frontend/` in its entirety: app shell, routing, Tailwind configuration, and the React Flow canvas. It is the only plan in wave 1 that touches that tree, and the only wave-2 plan that touches it afterwards is 02-07, which adds the WebSocket client sequentially.

Purpose: Every UI ticket from Phase 4 onward — Assurance Cards, Blast Radius, the Approval Centre, the Command Centre dial — lands inside this shell. Getting the routing table and the Tailwind v4 wiring right once here avoids restructuring it four phases later.
Output: A building, routed, styled, canvas-mounted SPA.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-foundation/02-RESEARCH.md
@CLAUDE.md
@BRANCHING.md
@frontend/README.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package legitimacy gate — verify the nine npm packages absent from the research audit table</name>
  <read_first>
    - `.planning/phases/02-foundation/02-RESEARCH.md` lines 117-137 — the Package Legitimacy Audit. Note which of this plan's packages it does and does not cover.
    - `.planning/phases/02-foundation/02-RESEARCH.md` lines 76-101 and 860-870 — the frontend stack table and Pitfall 3, the React Flow rename chain.
    - `CLAUDE.md` — Rule 13's general posture: never accept a generated name or claim as authoritative without checking its source.
  </read_first>
  <action>
Do not run `npm install` or `npm create` before this gate clears.

The research's Package Legitimacy Audit covers `vite`, `react`, `react-dom`, `tailwindcss`, `@xyflow/react`, and `vitest` with an `OK` verdict. It does **not** cover nine further packages this plan needs. Under the package-legitimacy protocol, a package absent from the audit table is treated as `[ASSUMED]` and cannot be installed on an agent's say-so — a plausible-looking package name is the single cheapest supply-chain attack against an AI-generated build.

This checkpoint is not deferrable to end-of-phase despite `workflow.human_verify_mode: end-of-phase`. Deferring it would mean the packages are already installed and their install scripts already executed by the time a human looks, which is precisely the outcome the gate exists to prevent.

Ask the developer to confirm each of the following resolves on npmjs.com to the maintainer and repository named, then to approve or reject the set:

| Package | Expected maintainer / repo | Check |
|---|---|---|
| `typescript` | Microsoft — github.com/microsoft/TypeScript | npmjs.com/package/typescript |
| `@vitejs/plugin-react` | Vite team — github.com/vitejs/vite-plugin-react | npmjs.com/package/@vitejs/plugin-react |
| `@tailwindcss/vite` | Tailwind Labs — github.com/tailwindlabs/tailwindcss | npmjs.com/package/@tailwindcss/vite |
| `react-router-dom` | Remix / React Router team — github.com/remix-run/react-router | npmjs.com/package/react-router-dom |
| `@testing-library/react` | Testing Library — github.com/testing-library/react-testing-library | npmjs.com/package/@testing-library/react |
| `@testing-library/jest-dom` | Testing Library — github.com/testing-library/jest-dom | npmjs.com/package/@testing-library/jest-dom |
| `jsdom` | jsdom org — github.com/jsdom/jsdom | npmjs.com/package/jsdom |
| `@types/react` | DefinitelyTyped — github.com/DefinitelyTyped/DefinitelyTyped | npmjs.com/package/@types/react |
| `@types/react-dom` | DefinitelyTyped — github.com/DefinitelyTyped/DefinitelyTyped | npmjs.com/package/@types/react-dom |

Flag two of these as decisions rather than mere confirmations, because they were chosen by the planner and are not named by any project source document:
- **`react-router-dom`** is a planner selection. Neither the Bible nor CLAUDE.md names a routing library; Section 11 describes eight pages but not how they are routed. If the developer prefers a different router, or prefers hash-based routing with no dependency at all, that choice is theirs to make here and the rest of this plan adapts to it — the route *table* is what matters, not the library.
- **`@testing-library/react` + `jsdom`** exist only to give UI-01 a real automated route-render assertion. Declining them is viable: the fallback is `npm run build` plus a non-DOM unit test over the exported route table, which is weaker evidence but still automated. Say so plainly rather than presenting the DOM stack as mandatory.

Also confirm the negative case from Pitfall 3: `react-flow-renderer` and `reactflow` are both superseded names and must not be installed. `@xyflow/react` v12 is the current package, and it uses a named `{ ReactFlow }` import rather than v11's default import.
  </action>
  <what-built>
Nothing yet — that is the point of this gate. No file has been written and no package manager has been invoked. What exists is a proposed dependency set for the frontend tier: six packages already carrying an `OK` verdict in the research Package Legitimacy Audit (`vite`, `react`, `react-dom`, `tailwindcss`, `@xyflow/react`, `vitest`), and nine that the audit does not cover and that this plan therefore treats as `[ASSUMED]` (`typescript`, `@vitejs/plugin-react`, `@tailwindcss/vite`, `react-router-dom`, `@testing-library/react`, `@testing-library/jest-dom`, `jsdom`, `@types/react`, `@types/react-dom`).

Two of the nine are genuine planner choices rather than mechanical consequences of the Vite template: `react-router-dom` (no project source names a routing library) and the `@testing-library/react` + `jsdom` pair (they exist only to make UI-01's route coverage a real render assertion instead of a build check).
  </what-built>
  <how-to-verify>
1. Open each of the nine npmjs.com pages listed in the table above.
2. For each, confirm the publisher and the linked source repository match the expected maintainer named in the table. A mismatch, an unfamiliar publisher, or a repository link that does not resolve is a reject.
3. Decide on `react-router-dom`: accept it, name a different router, or say you would rather have no routing dependency at all. The route *table* is what this plan needs; the library is replaceable and the rest of the plan adapts.
4. Decide on `@testing-library/react` + `jsdom`: accept them, or decline and take the weaker fallback (`npm run build` plus a non-DOM unit test over the exported route table).
5. Confirm that `react-flow-renderer` and `reactflow` — both superseded names for React Flow that a model could plausibly emit — are to be excluded.
  </how-to-verify>
  <resume-signal>
Reply with an approve or reject decision covering all nine packages, plus your choice on the two flagged decisions. Something as short as "approve all nine, keep react-router-dom, keep the DOM testing stack" is enough. If you reject or substitute anything, name the replacement so Task 2 installs that instead.
  </resume-signal>
  <verify>
    <human-check>
      The developer has opened each npmjs.com page listed above and confirmed the maintainer and linked repository match, and has explicitly approved or amended the `react-router-dom` and DOM-testing choices.
      Expected: an explicit approve/reject per row, with any substitution named.
      Why human: package legitimacy cannot be self-certified by the agent proposing the package. This is the one gate in the phase where the agent's own confidence is the wrong evidence.
    </human-check>
  </verify>
  <acceptance_criteria>
    - The developer has recorded an approve or reject decision covering all nine packages.
    - Any rejection or substitution is written into the plan's SUMMARY before Task 2 begins, and Task 2 uses the substituted package.
    - No `npm install`, `npm create`, or `npx` invocation has been run before this decision was recorded.
    - The set of packages installed in Task 2 is exactly the approved set plus the six already carrying an `OK` verdict in the research audit table — no extra package appears in `frontend/package.json` dependencies or devDependencies.
  </acceptance_criteria>
  <done>Every npm package this plan will install has either an audited `OK` verdict or an explicit human approval, and the two planner-chosen libraries have been consciously accepted or replaced.</done>
</task>

<task type="auto">
  <name>Task 2: Scaffold the Vite app with Tailwind v4 and React Flow v12, building clean on port 3000</name>
  <files>frontend/package.json, frontend/package-lock.json, frontend/index.html, frontend/vite.config.ts, frontend/tsconfig.json, frontend/tsconfig.app.json, frontend/tsconfig.node.json, frontend/src/main.tsx, frontend/src/App.tsx, frontend/src/index.css, frontend/vitest.setup.ts</files>
  <read_first>
    - `.planning/phases/02-foundation/02-RESEARCH.md` lines 76-116 — verified frontend package versions and the exact scaffold/install command sequence.
    - `.planning/phases/02-foundation/02-RESEARCH.md` lines 777-794 — the Tailwind v4 setup, which replaces the v3 `tailwind.config.js` + PostCSS + `@tailwind` directive pattern entirely.
    - `.planning/phases/02-foundation/02-RESEARCH.md` lines 860-870 — Pitfall 3 (React Flow package rename and the v12 named-import style) and lines 866-870 (Pitfall 4, the silent Tailwind v3/v4 mismatch).
    - `frontend/README.md` — the Phase 1 tier README stating the dev server runs on port 3000, and the server-trusted-rendering rule that governs everything built in this tree.
    - `.gitignore` — confirms `node_modules/` and `dist/` are ignored.
    - The Task 1 SUMMARY notes — the approved package set, including any substitution.
  </read_first>
  <action>
Scaffold into the existing `frontend/` directory, which already contains a committed `README.md` from Phase 1. Do not delete or overwrite that README; if `npm create vite@latest` refuses to scaffold into a non-empty directory, scaffold into a temporary sibling and move the generated files in, keeping the existing README intact.

Run `npm create vite@latest frontend -- --template react-ts`, then install: `tailwindcss` and `@tailwindcss/vite` as dev dependencies, `@xyflow/react` and the approved router as dependencies, and `vitest` plus the approved DOM-testing packages as dev dependencies. Pin the versions the research verified — vite 8.2.2, react and react-dom 19.2.8, tailwindcss 4.3.4, @xyflow/react 12.11.3 — and take the template's own defaults for `typescript`, `@vitejs/plugin-react`, and the `@types/*` packages rather than inventing version numbers for them. Confirm each install actually resolved the pinned version and record any drift in the SUMMARY.

Configure `frontend/vite.config.ts` to register both the React plugin and the Tailwind plugin, and to serve on port 3000 with `strictPort: true` so a port collision fails loudly instead of silently relocating the app to 3001 and breaking the WebSocket origin plan 02-07 depends on. Add the Vitest configuration in the same file: `environment: 'jsdom'`, `globals: true`, and `setupFiles: './vitest.setup.ts'`.

Set up Tailwind the v4 way and only the v4 way: no `tailwind.config.js`, no `postcss.config.js`, no `@tailwind base/components/utilities` directives. `frontend/src/index.css` gets the single line `@import "tailwindcss";`. This is the highest-risk step in the task, because the v3 pattern is far more common in circulation and fails *silently* — utility classes appear in the JSX, the build succeeds, and nothing is styled. Prove it works before moving on, using the visual check in the acceptance criteria rather than assuming.

Import the React Flow stylesheet once, in `frontend/src/main.tsx`: `@xyflow/react/dist/style.css`. Without it, v12 renders a collapsed, unusable canvas with no error message.

Create `frontend/vitest.setup.ts` importing the jest-dom matchers, if the DOM-testing packages were approved in Task 1.

Add npm scripts: `dev`, `build`, `preview`, and `test` mapped to `vitest run` so CI in plan 02-08 has a single non-interactive entry point. `vitest` in watch mode would hang a CI job forever; `vitest run` exits.

Commit `frontend/package-lock.json`. Plan 02-08's CI job uses `npm ci`, which requires it.
  </action>
  <verify>
    <automated>cd frontend && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npm run build` exits 0 and creates `frontend/dist/index.html`.
    - `cd frontend && npx tsc --noEmit` exits 0.
    - `frontend/vite.config.ts` imports and registers both `@vitejs/plugin-react` and `@tailwindcss/vite`, and sets `port: 3000` with `strictPort: true`.
    - `grep -c '@import "tailwindcss"' frontend/src/index.css` outputs `1`.
    - Neither `frontend/tailwind.config.js` nor `frontend/postcss.config.js` exists — Tailwind v4 needs neither, and their presence would indicate the v3 pattern was followed.
    - `grep -c "@xyflow/react/dist/style.css" frontend/src/main.tsx` outputs `1`.
    - `grep -cE '"(react-flow-renderer|reactflow)"' frontend/package.json` outputs `0` — neither superseded name was installed.
    - `frontend/package.json` scripts include `"test": "vitest run"`.
    - `frontend/README.md` still contains its Phase 1 `## Server-trusted rendering only` section — the scaffold extended the tier, it did not erase it.
    - Tailwind is proven live rather than assumed: with `npm run dev` running, a element carrying `class="bg-red-600 p-8"` renders with a red background in the browser. If it renders unstyled, the v4 plugin path is not wired and the task is not done.
    - `git status --porcelain frontend/` lists no `node_modules` or `dist` entry.
  </acceptance_criteria>
  <done>The app scaffolds, type-checks, builds, serves on port 3000, and Tailwind and React Flow are both genuinely wired rather than silently inert.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Route to all eight Bible Section 11 pages behind the prototype banner</name>
  <files>frontend/src/routes.tsx, frontend/src/App.tsx, frontend/src/components/PrototypeBanner.tsx, frontend/src/components/NavBar.tsx, frontend/src/components/AgentTopologyCanvas.tsx, frontend/src/pages/CommandCentre.tsx, frontend/src/pages/Copilot.tsx, frontend/src/pages/AuditReadiness.tsx, frontend/src/pages/Suppliers.tsx, frontend/src/pages/Actions.tsx, frontend/src/pages/AssuranceLab.tsx, frontend/src/pages/TrustCentre.tsx, frontend/src/pages/InspectionSimulator.tsx, frontend/src/__tests__/routes.test.tsx, frontend/README.md</files>
  <read_first>
    - `AegisX-AI-Project-Bible-v6.md` Section 11 (lines 1368-1402) — the page descriptions. Note the numbering skips 11.4 entirely, which is why there are eight documented pages and not seven.
    - `AegisX-AI-Project-Bible-v6.md` line 1374 and line 1574 — the exact banner string, which must be reproduced character for character including its em dashes.
    - `.planning/phases/02-foundation/02-RESEARCH.md` lines 756-775 — the resolved 7-vs-8 page discrepancy and the suggested route paths.
    - `frontend/vite.config.ts`, `frontend/src/main.tsx`, `frontend/src/index.css` from Task 2.
    - `frontend/README.md` — the server-trusted-rendering constraint that governs every page in this tree.
  </read_first>
  <behavior>
    - Navigating to each of `/`, `/copilot`, `/audit-readiness`, `/suppliers`, `/actions`, `/assurance-lab`, `/trust-centre`, `/inspection-simulator` renders that page's own heading, distinct from every other page's.
    - The prototype banner text is present on every one of the eight routes.
    - An unknown path renders a not-found page, not a blank screen or an unhandled error.
    - The `/copilot` route mounts a React Flow canvas containing placeholder nodes.
    - The exported route table has exactly eight entries plus the catch-all, and adding a ninth requires appending one entry and no structural change.
  </behavior>
  <action>
Create `frontend/src/routes.tsx` exporting a single route table as a plain array of `{ path, label, element }` objects, consumed by the router in `App.tsx`. Exporting the table as data rather than burying `<Route>` elements in JSX matters for two reasons: the navigation bar derives from the same source so a route can never exist without a link, and the route-render test asserts against it directly.

The eight routes, each mapped to its Bible section:

| Path | Page component | Bible |
|---|---|---|
| `/` | `CommandCentre` | 11.1 |
| `/copilot` | `Copilot` | 11.2 |
| `/audit-readiness` | `AuditReadiness` | 11.3 |
| `/suppliers` | `Suppliers` | 11.5 |
| `/actions` | `Actions` | 11.6 |
| `/assurance-lab` | `AssuranceLab` | 11.7 |
| `/trust-centre` | `TrustCentre` | 11.8 |
| `/inspection-simulator` | `InspectionSimulator` | 11.9 |

Bible Section 11 has no subsection 11.4 — the numbering jumps from 11.3 to 11.5. The missing page is almost certainly Blast Radius, which gets its own ticket (SENT-3-04) in Phase 4. Leave a comment in the route table recording this and stating that Phase 4 appends a ninth `/blast-radius` entry here. SENT-1-07's contract says "the 7 pages"; the Bible documents eight; the Bible wins (CLAUDE.md Rule 14).

Create one page component per route under `frontend/src/pages/`. Each renders its own `h1` naming the page and a short paragraph, taken from that page's Bible Section 11 description, saying what the page will eventually show. These are placeholders in *content* only — they are real components on real routes, not a shared stub switched by a prop. Do not fetch from the backend: no page in this plan calls an API, because only `/api/health` exists and none of these pages consumes it.

Create `frontend/src/components/PrototypeBanner.tsx` rendering the string `PROTOTYPE — SYNTHETIC DATA — NOT VALIDATED FOR PRODUCTION GxP USE` on a red background, styled with Tailwind utilities. Reproduce the string exactly as Bible line 1374 writes it, em dashes included; Bible Section 15's limitations list makes this banner a stated compliance control, not decoration. Render it in the app layout above the router outlet so it persists across every route rather than being repeated per page.

Create `frontend/src/components/NavBar.tsx` deriving its links from the exported route table.

Create `frontend/src/components/AgentTopologyCanvas.tsx` wrapping `@xyflow/react`'s `ReactFlow` with the v12 named import. Seed it with placeholder nodes for the eleven graph participants — `C2`, `A0`, `A1` through `A6`, `C1`, `A7`, `C3` — and placeholder edges matching the Bible Section 1.2 topology `C2 → A0 → [A1…A6] → C1 → A7 → C3`. Laying the placeholders out in the real topology now, rather than as three anonymous boxes, means Phase 6's live agent-state streaming replaces node *colours* rather than the whole component. Give the wrapper a fixed height; React Flow renders nothing inside a zero-height parent, which is the most common way this component appears broken while erroring in no way at all. Mount it on the `/copilot` page.

Create `frontend/src/__tests__/routes.test.tsx` covering the `<behavior>` list: render the app at each of the eight paths through the router's memory history and assert that path's distinctive heading, assert the banner is present on every one of them, assert the unknown-path case, and assert the route table's length. If Task 1 declined the DOM-testing packages, fall back to asserting the exported route table's shape and the uniqueness of its paths and labels, and record in the SUMMARY that render coverage was not taken.

Extend `frontend/README.md` with a `## Shell (Stage 1)` section: the route table, the Tailwind v4 note that no config file is expected, the React Flow v12 named-import and stylesheet requirements, the dev/build/test commands, and a pointer that Phase 4 appends the Blast Radius route.
  </action>
  <verify>
    <automated>cd frontend && npm run build && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npm run build` exits 0 and `cd frontend && npm test` exits 0 with every route test passing.
    - `cd frontend && npx tsc --noEmit` exits 0.
    - `grep -c "PROTOTYPE — SYNTHETIC DATA — NOT VALIDATED FOR PRODUCTION GxP USE" frontend/src/components/PrototypeBanner.tsx` outputs `1`.
    - All eight path strings `'/'`, `'/copilot'`, `'/audit-readiness'`, `'/suppliers'`, `'/actions'`, `'/assurance-lab'`, `'/trust-centre'`, `'/inspection-simulator'` appear in `frontend/src/routes.tsx`.
    - `ls frontend/src/pages/*.tsx | wc -l` outputs `8`.
    - `grep -c "import { ReactFlow" frontend/src/components/AgentTopologyCanvas.tsx` outputs `1` — the v12 named import, not a v11 default import.
    - All eleven node ids `C2`, `A0`, `A1`, `A2`, `A3`, `A4`, `A5`, `A6`, `C1`, `A7`, `C3` appear in `frontend/src/components/AgentTopologyCanvas.tsx`.
    - With `npm run dev` running, visiting each of the eight routes in a browser shows that page's own heading, the red banner, and — on `/copilot` — a visible React Flow canvas with laid-out nodes rather than an empty region.
    - Deliberately breaking one page component's heading string makes `npm test` fail and name that route. Restore it afterward.
  </acceptance_criteria>
  <done>Eight distinct routed pages render behind a persistent prototype banner, with a React Flow canvas laid out in the real agent topology, all covered by an automated suite that can fail.</done>
</task>

</tasks>

<artifacts_produced>
| Artifact | Kind | Notes |
|---|---|---|
| `frontend/package.json` + `package-lock.json` | manifest | Vite 8.2.2, React 19.2.8, Tailwind 4.3.4, @xyflow/react 12.11.3, plus the Task-1-approved set. Scripts: `dev`, `build`, `preview`, `test` (`vitest run`). |
| `frontend/vite.config.ts` | config | React + Tailwind v4 plugins; port 3000 `strictPort`; Vitest jsdom config. |
| `frontend/src/routes.tsx` | TS | Exported route table — 8 entries, room for a 9th (Blast Radius, Phase 4). |
| `frontend/src/pages/*.tsx` | TSX | `CommandCentre`, `Copilot`, `AuditReadiness`, `Suppliers`, `Actions`, `AssuranceLab`, `TrustCentre`, `InspectionSimulator`. |
| `frontend/src/components/PrototypeBanner.tsx` | TSX | The Bible-mandated persistent red banner. |
| `frontend/src/components/NavBar.tsx` | TSX | Derived from the route table. |
| `frontend/src/components/AgentTopologyCanvas.tsx` | TSX | React Flow v12 canvas, 11 placeholder nodes in the C2→A0→[A1..A6]→C1→A7→C3 topology. |
| `frontend/src/__tests__/routes.test.tsx` | test | UI-01 coverage. |
| `frontend/README.md` | doc | Extended with `## Shell (Stage 1)`. |

**Consumed downstream:** plan 02-07 (attaches the WebSocket client to `Copilot.tsx`), plan 02-08 (CI runs `npm ci && npm run build && npm test`), Phase 4 SENT-3-02/3-04/3-05 (evidence graph, Blast Radius, Assurance Cards render inside this shell), Phase 6 SENT-5-01 (Command Centre replaces its placeholder).
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → `frontend/node_modules` | Third-party code executes at install time via lifecycle scripts and at runtime in the browser. This is the largest new attack surface the phase introduces. |
| browser → backend `/api/*` | Not crossed by this plan: no page here makes a network request. It becomes live in plan 02-07. |
| server-produced data → rendered UI | Not crossed yet, but the tier's governing rule — approval dialogs and evidence cards render only from server-trusted data, never LLM-generated markup — is established in `frontend/README.md` and applies from the first page that renders real data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-SC | Tampering | npm install supply chain | high | mitigate | Six packages carry an `OK` verdict in the research Package Legitimacy Audit. The nine that do not are treated as `[ASSUMED]` and gated behind the blocking human checkpoint in Task 1, which runs before any install command. `package-lock.json` is committed so CI installs the same resolved tree. `react-flow-renderer` and `reactflow`, both superseded names a model could plausibly emit, are explicitly excluded and grep-asserted absent. |
| T-02-15 | Spoofing | typosquatted or hallucinated package name | high | mitigate | Task 1 requires a human to confirm each un-audited package's maintainer and repository on its registry page. The agent proposing a package is never the authority that approves it. |
| T-02-16 | Tampering | LLM-generated markup rendered as UI | medium | accept | No page in this plan renders model-produced content — every string is authored in source. The control that matters (`ActionProposal` dialogs built only from server-trusted metadata) is enforced at the C3 Action Gateway in Phase 5, SENT-4-03/4-08; `frontend/README.md` states the constraint now so it is in place before the first real render. |
| T-02-17 | Information Disclosure | dev server exposed beyond localhost | low | mitigate | Vite binds to localhost by default and this plan sets no `host` option, matching the loopback-only publishing posture Phase 1 established for every service. |
| T-02-18 | Denial of Service | `strictPort` failure on port collision | low | accept | Failing loudly on a taken port is the intended behaviour: silently relocating to 3001 would break the WebSocket origin plan 02-07 assumes and produce a confusing failure two plans later instead of an obvious one here. |
</threat_model>

<verification>
Run from the repo root. No backend, database, or OPA service is required — nothing in this plan makes a network call.

1. `cd frontend && npm ci` — installs from the committed lockfile without error.
2. `cd frontend && npx tsc --noEmit` — exits 0.
3. `cd frontend && npm run build` — exits 0, produces `dist/index.html`.
4. `cd frontend && npm test` — exits 0, all route tests pass.
5. `cd frontend && npm run dev`, then in a browser: visit all eight routes and confirm each shows its own heading and the red prototype banner, and that `/copilot` shows a laid-out React Flow canvas.
6. Tailwind live check: confirm a utility-styled element actually renders styled. A silently inert Tailwind v4 setup passes every command above.
7. Failure-path check: break one page heading, confirm `npm test` fails and names that route, restore, re-run green.
</verification>

<success_criteria>
- The React + TypeScript + Vite + Tailwind app boots on port 3000, builds clean, and type-checks clean (UI-01).
- All eight documented Bible Section 11 routes render distinct pages, with the mandated prototype banner persistent across all of them (UI-01).
- A React Flow v12 canvas is mounted with placeholder nodes laid out in the Section 1.2 agent topology (UI-01).
- Every installed npm package is either audited `OK` or explicitly human-approved; no superseded React Flow package name appears in the manifest.
- No file outside `frontend/` was touched; `docker-compose.yml`, `.env.example`, root `README.md`, and `BRANCHING.md` are unmodified (BRANCHING.md §5).
</success_criteria>

<output>
Create `.planning/phases/02-foundation/02-04-SUMMARY.md` when done.
</output>
