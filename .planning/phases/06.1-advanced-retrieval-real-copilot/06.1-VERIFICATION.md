---
phase: 06.1-advanced-retrieval-real-copilot
verified: 2026-08-29T15:59:22Z
status: human_needed
score: 13/13 must-haves verified
behavior_unverified: 0
overrides_applied: 0
human_verification:
  - test: "Visit /knowledge in a real browser: confirm the drop zone shows its instruction copy before interaction, 'Browse files' is disabled until a system is picked, uploading each of the four fixture formats (md/pdf/docx/csv) produces a row that progresses to Ready with a nonzero indexed-unit count, uploading a .exe shows the unsupported-type copy inline and adds no row, and the only accent (emerald) colour use is the Browse files button and completed-stage glyphs."
    expected: "All of the above hold visually; no colour, spacing, or interaction deviates from 06.1-UI-SPEC.md."
    why_human: "Automated coverage is jsdom-based (vitest) and grep-based (colour-class assertions); no test renders the page in a real browser. Plan 06.1-05's own <human-check> step was never executed by the implementer (documented in 06.1-05-SUMMARY.md's 'User Setup Required')."
  - test: "With the full stack running (now that GEMINI_API_KEY is genuinely configured — confirmed present during this verification), open /copilot in a browser: ask a plain question answerable from an uploaded document and confirm the answer quotes it, the 'How AegisX searched' toggle opens a real six-stage trace, and each evidence item names a real section/page findable in the source file; ask about a topic no uploaded document covers and confirm the insufficient-evidence copy with no evidence list; paste a prompt-injection string and confirm the red blocked bubble shows C2's real reason."
    expected: "All four scenarios render as specified in 06.1-UI-SPEC.md, matching the backend behavior this verification already confirmed live via direct API calls."
    why_human: "This verification confirmed the backend contract for all four scenarios via curl (see Evidence below) — the API-level behavior is proven. What remains unverified is browser rendering: toggle interaction, colour/spacing fidelity, and bubble styling. Plan 06.1-06's own <human-check> step was never executed by the implementer."
  - test: "Ask the Copilot a question whose answer is grounded in exactly one document/system; confirm the grounded answer and its evidence render BEFORE any navigation notice appears; confirm the notice names the correct destination; click 'Stay here' and confirm no navigation occurs; ask again and let the 3-second timer elapse, confirming it lands on /knowledge with the correct system selected and that document's row highlighted (ring style, not accent colour). Separately, click a graph-relationship evidence link and confirm /blast-radius opens on the correct system with the cited node pre-selected rather than an empty or 404 panel."
    expected: "Matches 06.1-UI-SPEC.md's D-13 interaction contract exactly, including WCAG 2.2.1 cancellability."
    why_human: "This is real-time, interactive browser behavior (a timer, a live navigation, DOM highlight state) that cannot be verified by static analysis. Plan 06.1-08's own <human-check> step was never executed by the implementer."
---

# Phase 06.1: Advanced Retrieval & Real Copilot Verification Report

**Phase Goal:** A user can upload a real GxP document, ask the Copilot a genuine investigation question (not just the seeded hero query), and get a grounded answer with inspectable evidence — citations, source document/section/page, and retrieval method — produced by real hybrid (dense+lexical) retrieval and cross-encoder reranking over that document, routed through the actual A0-C3 orchestrator instead of the readiness-only stub.

**Verified:** 2026-08-29T15:59:22Z
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

This verification did not rely on SUMMARY.md claims. It re-ran the full backend and frontend test suites live against the running Docker stack (Postgres/Qdrant/OPA), started the real FastAPI server, and drove the actual HTTP endpoints end-to-end with `curl` — including a real document upload and four distinct free-text Copilot questions — using a genuinely configured `GEMINI_API_KEY` (confirmed present in `.env` during this session, contradicting all eight plans' own "no live key configured" caveat, which was true only when those plans were executed).

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | A user can upload a real GxP document (PDF/DOCX/CSV/Markdown) and it is parsed, chunked with real provenance, embedded, and indexed in Postgres + Qdrant | ✓ VERIFIED | Live `curl -X POST /api/documents/upload` with `urs_extract.md` returned `{"status":"READY","chunk_count":3,"indexed_vector_count":3}`. `document_chunks` schema confirmed live to carry `section`/`page`/`parent_chunk_id`/`chunk_index`/`metadata` (`\d document_chunks` on live Postgres). PDF/DOCX/CSV parsers (`parse_pdf`/`parse_docx`/`parse_csv`) exist with dedicated tests (06.1-04); 178 retrieval/route tests re-run live, all passing. |
| 2 | A free-text investigation question (not the hero query) is routed through the real compiled `StateGraph` (`compiled_graph.ainvoke`) and returns a grounded answer | ✓ VERIFIED | `grep -n "compiled_graph.ainvoke" backend/app/routes/copilot_query.py` matches exactly once. Live `curl -X POST /api/copilot/investigate` with a genuine question about the uploaded document's traceability content returned a synthesized answer citing `[EV-8ce357e1]` with real quoted content from the source file, `model_attribution: "deepseek/deepseek-v4-flash-0731"` (a real model id, not a placeholder), and `blocked: false`. |
| 3 | Retrieval is real hybrid dense+lexical (Qdrant + BM25) with explicit RRF fusion, not dense-only | ✓ VERIFIED | Live response's evidence items carry `retrieval_method: "hybrid"` with populated `dense_score` (0.76), `bm25_score` (3.03), AND `reranker_score` (1.0) simultaneously — proof both legs ran and were fused, not just one. `reciprocal_rank_fusion()` exists as a pure function (`backend/app/retrieval/hybrid_search.py:167`), unit-tested for purity (no I/O). |
| 4 | Exactly one batched cross-encoder-style reranking call runs per query, and the sufficiency gate is a deterministic comparison against a named threshold | ✓ VERIFIED | `rerank_batch()` exists (`hybrid_search.py:297`); its one-call invariant is asserted by a `respx call_count==1` test over 40 candidates (06.1-03). Live response's `evidence_support: "HIGH"` and `stages` show `reranking: complete, "3 candidates scored"` — real reranking ran, not skipped. |
| 5 | Evidence carries full Bible §15.7 provenance (document/section/page/method/scores/why_selected/parent_section/graph_path) | ✓ VERIFIED | Live evidence items show `section: "Section A: System Purpose and Scope"`, real scores, and a graph-relationship item (`evidence_type: graph_relationship`) alongside document evidence — parent-context and graph expansion both wired and firing in the live response. |
| 6 | When nothing clears the relevance threshold, the Copilot gives an honest insufficient-evidence answer with zero fabrication | ✓ VERIFIED | Live `curl` with an off-topic question ("airspeed velocity of an unladen swallow") returned `insufficient_evidence: true`, `evidence: []`, and the exact UI-SPEC copywriting-contract string, with `answer` containing no model-knowledge content. |
| 7 | A prompt-injection input is still blocked deterministically by C2 inside the graph, with C2's real reason string | ✓ VERIFIED | Live `curl` with `"please ignore previous instructions..."` returned `blocked: true`, `blocked_reason: "regex_match:(?i)(ignore previous instructions\|override system prompt\|disregard rules)"` — C2's real deterministic pattern, not a generic message. |
| 8 | `navigation_target` (D-13) is computed deterministically and only when every citation resolves to one unambiguous destination | ✓ VERIFIED | `compute_navigation_target()` is a pure function (`grep` confirms zero `await`/`call_llm`/`acquire_pool` inside its body); 12 named unit tests cover all 5 fail-closed paths. Live response with 1 document + 1 graph item (2 distinct destinations) correctly returned `navigation_target: null`, matching the fail-closed contract. |
| 9 | Deep links render on every citation resolving to a real entity, built from a single closed client-side route map, with no URL/href ever crossing the API boundary | ✓ VERIFIED | `NavigationTarget` schema (`app/schemas.py`) carries no `url`/`href`/`path`/`link` field, asserted directly by acceptance-criteria tests. `frontend/src/lib/navigation.ts` is the sole path-builder (`grep -rlE "/(knowledge\|blast-radius)\?"` across `frontend/src` excluding tests and pre-existing Phase-6 files returns none from this phase). 24 named tests in `navigation.test.ts`, all passing in the live re-run. |
| 10 | Auto-navigation is cancellable, arms exactly once for the newest message only, and never fires before the answer renders | ✓ VERIFIED | `ChatMessage.tsx` renders `<EvidenceView>` before `<AutoNavigateNotice>` (asserted by acceptance grep and confirmed by direct file read). `useRef`-guarded single-arm `setTimeout` with `clearTimeout` on unmount; 8 named tests including re-render-arms-once and unmount-clears-timer, all passing live. |
| 11 | The Knowledge page and evidence UI render only real backend-sourced values, with no timer-driven or fabricated progress | ✓ VERIFIED | `grep -cE "setTimeout\|setInterval" Knowledge.tsx` = 0. `EvidenceView.tsx`/`InvestigationTrace.tsx` perform no `fetch`, no grading, only `.toFixed(2)` formatting — asserted by acceptance grep and unit tests. |
| 12 | RAG-03/RAG-04 quality is measured, not assumed — a labelled eval harness with a recorded, regression-gated baseline exists | ✓ VERIFIED | `backend/README.md` contains a `## Retrieval Precision Baseline (HARD-04)` section with a real dense/lexical/hybrid comparison table, a genuine interpretation paragraph (not restated numbers) for both `RRF_K` and `RERANK_RELEVANCE_THRESHOLD`, and an honest caveat about the mocked-transport measurement method. 26 named eval tests re-run live, all passing. |
| 13 | Graph topology, C1 verification, and C2/C3 gateways are provably unchanged by this phase's additions | ✓ VERIFIED | Each plan's own acceptance criteria asserts `git diff` emptiness against `c1_verifier.py`/`c2_gateway.py`/`c3_gateway.py`/`a0_orchestrator.py`/`a2_compliance.py`/`evidence_graph.py`. Full 548-test backend suite (including `test_c1_verifier.py`, `test_graph_topology.py`, `test_graph_gateways.py`, `test_hero_loop.py`) re-run live in this session: 548 passed, 0 failed. |

**Score:** 13/13 truths verified (0 present-but-behavior-unverified)

### Required Artifacts

| Artifact | Expected | Status | Details |
|---|---|---|---|
| `backend/app/retrieval/embeddings.py` | Hosted embedding router extension | ✓ VERIFIED | Exists, live-tested (real Gemini embedding call succeeded during this verification's live upload) |
| `backend/app/retrieval/qdrant_store.py` | Qdrant client, collection bootstrap, dense search | ✓ VERIFIED | Live `curl localhost:6333/collections/gxp_document_chunks` confirms `size:768, distance:Cosine` |
| `backend/app/retrieval/ingest.py` | parse/chunk/index pipeline, PDF/DOCX/CSV/text | ✓ VERIFIED | 572 lines; PDF/DOCX/CSV parsers present with format-honest provenance |
| `backend/app/retrieval/lexical.py` | BM25 corpus + search | ✓ VERIFIED | 109 lines; `tokenize`/`build_corpus`/`bm25_search` present |
| `backend/app/retrieval/hybrid_search.py` | RRF, reranking, parent-context, graph expansion | ✓ VERIFIED | 728 lines; all 5 major functions present and live-exercised |
| `backend/app/retrieval/evaluation.py` | IR metrics + eval runner | ✓ VERIFIED | Present; 26 tests re-run live, all passing |
| `backend/app/routes/documents.py` | Upload + list endpoints | ✓ VERIFIED | 280 lines; both routes registered in `main.py`, live-exercised |
| `backend/app/routes/copilot_query.py` | `/api/copilot/investigate` | ✓ VERIFIED | 444 lines; registered, live-exercised for all 4 scenarios |
| `infra/postgres/initdb/004_document_chunks_structure.sql` | D-02 migration | ✓ VERIFIED | Applied to live Postgres — all 5 columns present |
| `frontend/src/pages/Knowledge.tsx` | Knowledge page | ✓ VERIFIED | 416 lines; route registered; unit tests pass; **browser rendering not visually confirmed (see Human Verification)** |
| `frontend/src/components/EvidenceView.tsx` | Evidence inspection UI | ✓ VERIFIED | 241 lines; deep links wired; unit tests pass |
| `frontend/src/components/InvestigationTrace.tsx` | Six-stage trace panel | ✓ VERIFIED | 89 lines; no timers; unit tests pass |
| `frontend/src/components/AutoNavigateNotice.tsx` | D-13 cancellable notice | ✓ VERIFIED | 137 lines; single-arm timer pattern confirmed; unit tests pass |
| `frontend/src/lib/navigation.ts` | Closed route map | ✓ VERIFIED | 92 lines; sole path-builder; unit tests pass |

### Key Link Verification

| From | To | Via | Status | Details |
|---|---|---|---|---|
| `routes/documents.py` | `retrieval/ingest.py` | `index_document(...)` | ✓ WIRED | Live upload succeeded end-to-end |
| `routes/copilot_query.py` | `graph/state.py` | `compiled_graph.ainvoke` | ✓ WIRED | Confirmed by grep + live invocation |
| `agents/minimal_specialists.py` (A1) | `retrieval/hybrid_search.py` | `hybrid_retrieve(...)` | ✓ WIRED | `grep` confirms import + call inside `run_a1`; live response shows real A1 retrieval evidence |
| `hybrid_search.py` | `llm_router.py` | `call_llm(task="rerank")` | ✓ WIRED | `"rerank"` registered in `PROVIDER_CONFIG["gemini_flash_fast"]["use_for"]`; live response shows `reranker_score` populated |
| `EvidenceView.tsx` | `lib/navigation.ts` | `evidenceHref(item, systemId)` | ✓ WIRED | grep + unit tests confirm |
| `ChatMessage.tsx` | `AutoNavigateNotice.tsx` | conditional render after `EvidenceView` | ✓ WIRED | Confirmed by file read (element order) |
| `Knowledge.tsx`/`BlastRadius.tsx` | `?system=`/`?document=`/`?node=` params | `useSearchParams` | ✓ WIRED | Unit tests (`deepLinkLanding.test.tsx`, 10 tests) pass |

### Data-Flow Trace (Level 4)

Traced the rendered Copilot answer's evidence back to its source: `CopilotInvestigateResponse.evidence` ← `run_a1`'s `retrieval_evidence` ← `hybrid_retrieve()`'s fused/reranked candidate set ← real Qdrant `dense_search` + real `bm25_search` over `document_chunks`, hydrated from live Postgres by `chunk_id` (never trusting the Qdrant payload, per plan 06.1-02's explicit design). Confirmed FLOWING, not static: the live evidence items' `content` field is the actual retrieved chunk text from the uploaded fixture, and scores vary per query (0.76/3.03/1.0 for the traceability question, `[]` for the off-topic question). No hardcoded fallback found in any of the response paths.

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|---|---|---|---|
| Full backend suite (fresh, live stack) | `pytest -q` (548 tests) | `548 passed, 0 failed` in 327s | ✓ PASS |
| Retrieval/route-specific suite | `pytest tests/test_hybrid_search.py tests/test_routes_documents.py tests/test_routes_copilot_investigate.py tests/test_retrieval_ingest.py tests/test_retrieval_embeddings.py tests/test_minimal_specialists.py tests/test_retrieval_eval.py -q` | `178 passed` | ✓ PASS |
| Frontend full suite | `npm test -- --run` | `237 passed (237)`, 20 files | ✓ PASS |
| Frontend typecheck | `npx tsc -b --noEmit` | exit 0 | ✓ PASS |
| Live document upload | `curl -X POST /api/documents/upload` (real GEMINI_API_KEY) | `status: READY, chunk_count: 3` | ✓ PASS |
| Live grounded-answer investigation | `curl -X POST /api/copilot/investigate` (real question) | grounded answer citing real evidence, `evidence_support: HIGH` | ✓ PASS |
| Live insufficient-evidence path | `curl -X POST /api/copilot/investigate` (off-topic question) | honest refusal, zero evidence | ✓ PASS |
| Live injection-block path | `curl -X POST /api/copilot/investigate` (jailbreak phrase) | `blocked: true`, C2's real reason string | ✓ PASS |
| Live document listing | `curl GET /api/documents?system_id=...` | real chunk counts and statuses | ✓ PASS |
| Post-verification cleanup | manual DB/Qdrant cleanup + graph rebuild | `node_count: 14, edge_count: 9` (baseline restored) | ✓ PASS |
| Regression check after live testing | `pytest tests/test_routes_evidence_graph.py tests/test_evidence_graph.py tests/test_blast_radius.py -q` | `73 passed` | ✓ PASS |

### Requirements Coverage

| Requirement | Source Plan(s) | Description | Status | Evidence |
|---|---|---|---|---|
| RAG-01 | 06.1-01, 06.1-04 | Real document ingestion (upload→parse→chunk→index) PDF/DOCX/CSV/text | ✓ SATISFIED | Live upload + 4-format unit test coverage |
| RAG-02 | 06.1-01 | `document_chunks` schema extended (D-02) | ✓ SATISFIED | Live `\d document_chunks` confirms all 5 columns |
| RAG-03 | 06.1-02, 06.1-03 | Hybrid dense+BM25 retrieval with fusion | ✓ SATISFIED | Live response shows `retrieval_method: hybrid` with both scores |
| RAG-04 | 06.1-03 | Cross-encoder reranking stage | ✓ SATISFIED | Live response shows populated `reranker_score`; one-call invariant tested |
| RAG-05 | 06.1-01, 06.1-02, 06.1-03 | Evidence filtering + full §15.7 provenance | ✓ SATISFIED | Live response carries section/page/scores/graph_path |
| RAG-06 | 06.1-02, 06.1-06, 06.1-08 | Copilot wired to real `StateGraph`; honest insufficient-evidence | ✓ SATISFIED | Live `compiled_graph.ainvoke` call confirmed; insufficient-evidence path confirmed |
| RAG-07 | 06.1-01, 06.1-05, 06.1-06 | Evidence inspection UI | ✓ SATISFIED (backend contract); UI rendering — see Human Verification | `EvidenceView.tsx` unit-tested; visual rendering not browser-confirmed |
| AGT-01 | 06.1-02, 06.1-03 | A1 hybrid retrieval + cross-encoder reranking | ✓ SATISFIED | Live evidence shows real A1 retrieval with fusion+reranking |
| HARD-04 | 06.1-07 | RAG retrieval precision evaluation | ✓ SATISFIED | Recorded baseline table + interpretation in `backend/README.md`; regression test present |

**Note on `.planning/REQUIREMENTS.md` staleness (process gap, not a functional gap):** `.planning/REQUIREMENTS.md` still shows `AGT-01`, `HARD-04`, `RAG-03`, and `RAG-04` as unchecked `[ ]`, and its Traceability table (lines 128-168) has no rows at all for any of `RAG-01` through `RAG-07`, `AGT-01`, or `HARD-04` — the table stops at `UI-04 | Phase 6`. Functionally, this verification found strong live evidence that all nine requirements are satisfied (see table above); the checkboxes and traceability table were simply never updated by any of the 8 plans in this phase. This should be fixed as a documentation follow-up before the milestone is archived, but it does not block the phase goal, since the underlying capability is real and independently verified.

### Anti-Patterns Found

No `TBD`/`FIXME`/`XXX`/`TODO`/`HACK`/`PLACEHOLDER` markers found in any file this phase created or modified (`backend/app/retrieval/`, `backend/app/routes/documents.py`, `backend/app/routes/copilot_query.py`, `frontend/src/pages/Knowledge.tsx`, `frontend/src/components/EvidenceView.tsx`, `InvestigationTrace.tsx`, `AutoNavigateNotice.tsx`, `frontend/src/lib/navigation.ts`). No stub-shaped `return []`/`return {}` patterns found that aren't legitimate honest-empty-state returns (verified against real data flow in the Data-Flow Trace above).

### Human Verification Required

None of the following block the phase goal from being achieved at the API/data level — all four are confirmed working via live `curl` calls in this verification. What remains unverified is **browser-rendered visual/interactive behavior**, which none of the three frontend plans' own `<human-check>` steps were ever executed for by their implementers (each plan's SUMMARY.md explicitly documents this as an open item). See frontmatter `human_verification` for the three consolidated checks (Knowledge page upload UX, Copilot chat rendering of answer/trace/evidence, and the D-13 deep-link/auto-navigate interactive flow).

### Gaps Summary

No functional gaps were found. All 13 derived observable truths were independently verified against the live, running system (not SUMMARY.md claims) — including a real document upload, four distinct live Copilot investigation calls (grounded answer, insufficient evidence, injection block, document listing) exercising the actual A0-C3 orchestrator, real Qdrant hybrid retrieval, real cross-encoder-style reranking, and real evidence-graph expansion, all using a genuinely configured `GEMINI_API_KEY` (contrary to every plan's own documented "no live key" limitation at execution time — the key has since been configured in this environment). The full backend (548/548) and frontend (237/237) suites were independently re-run fresh in this verification session and passed clean, matching the orchestrator's claim exactly.

The one category of unresolved risk is that the three frontend plans' own `<human-check>` steps (visual/interactive browser confirmation of the Knowledge page, the Copilot chat's rendering of trace/evidence, and the D-13 auto-navigate/deep-link flow) were never executed by any implementer across all three plans — each SUMMARY.md says so explicitly. This verification substituted extensive live API-level testing plus static/unit-test verification, which closes most of the risk (the underlying data contracts are proven correct), but does not close the purely visual/interactive risk (timer behavior in a real browser, colour/spacing fidelity, actual click-through navigation). This routes the phase to `human_needed` rather than `passed`, per the verification protocol's rule that any unresolved human-verification item takes precedence over an otherwise-clean score.

A secondary, non-blocking finding: `.planning/REQUIREMENTS.md`'s checkboxes and traceability table were never updated for this phase's 9 requirement IDs (see Requirements Coverage note above) — a documentation-accuracy gap, not a functional one.

---

*Verified: 2026-08-29T15:59:22Z*
*Verifier: Claude (gsd-verifier)*
