---
phase: 06.1.1-blast-radius-investigation-redesign
plan: 03
type: execute
wave: 3
depends_on: ["06.1.1-02"]
files_modified:
  - frontend/src/pages/BlastRadius.tsx
  - backend/README.md
autonomous: false
requirements: []
estimate:
  tokens: 20000
  raw_tokens: 20000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A live Copilot investigation question whose evidence includes a graph_relationship item terminating at a real node produces a navigation_target of kind graph_node, and following it lands on the redesigned Blast Radius investigation view (Wave 1/2 output) with that node pre-selected — verified against the running Docker stack (Postgres/Qdrant/OPA) with a real document/graph already loaded, not asserted from reading code alone"
    - "If this live check reveals a genuine gap (e.g. no graph_relationship evidence item is ever surfaced for an explicit imperative like 'show me the blast radius of X'), the gap is reported in this plan's execution notes and backend/README.md as a documented limitation — not silently patched with new backend intent-classification work, which would exceed this phase's frontend-only mandate (D-12) and Phase 06.1's already-closed scope"
  artifacts: []
  key_links:
    - from: "frontend/src/pages/BlastRadius.tsx"
      to: "frontend/src/lib/navigation.ts"
      via: "the existing NAVIGATION_ROUTE_BUILDERS.graph_node builder already produces the ?system=&node= URL this page reads — this task verifies, does not rebuild, that round-trip"
      pattern: "searchParams.get\\('node'\\)"
---

<objective>
Wave 3 (gated, non-autonomous — requires a human or a live-service session to actually drive the Copilot
and observe the result): verify D-09's Copilot-navigation requirement end-to-end using the infrastructure
Phase 06.1 already shipped (`compute_navigation_target()`, `navigation.ts`'s `graph_node` route builder,
`AutoNavigateNotice`) plus this phase's Wave 1/2 redesign of the landing page itself. Per
06.1.1-RESEARCH.md's D-09 findings, the navigation *plumbing* already exists — this wave's job is to prove
it actually fires for a genuine "investigate the blast radius of X" question against a real uploaded
document/graph, and that the user lands on the *redesigned* investigation view (not the old passive panel),
with the origin node correctly reflected in the new investigation header (Wave 1) and pre-selected for path
highlighting (Wave 2).

This plan is marked `autonomous: false` because its core verification step is inherently a live,
interactive check (asking the Copilot a real question and observing where the browser/API actually lands)
that the project's own established pattern (06.1's VERIFICATION.md) treats as requiring a live curl/browser
session, not something a code-only executor can complete unattended. If run inside an autonomous pipeline
that cannot drive a live Copilot query, this plan must stop and report `human_needed` rather than fabricate
a pass — matching the project's existing verification discipline (see 06.1-VERIFICATION.md's own
`human_verification` entries for the identical class of gap).
</objective>

<tasks>
<task id="1" type="execute">
<read_first>
- backend/app/routes/copilot_query.py (compute_navigation_target call site, to confirm the live endpoint to hit)
- frontend/src/lib/navigation.ts (graph_node route builder, unchanged reference)
- frontend/src/pages/BlastRadius.tsx (as modified through Wave 2 — the actual landing state this must verify)
- .planning/phases/06.1.1-blast-radius-investigation-redesign/06.1.1-RESEARCH.md (D-09 section)
</read_first>
<action>
With the Docker stack running (Postgres 5432 / Qdrant 6333 / OPA 8181) and the backend server started, upload a
real document containing at least one entity that also exists in the evidence graph (or use an already-loaded
demo system's document + graph pairing), then `curl -X POST /api/copilot/investigate` with a question phrased as
an explicit blast-radius request (e.g. "What is the blast radius of RISK:R-004?" or the closest real entity id
present in the loaded graph — confirm the id via `GET /api/systems/{system_id}/evidence-graph` first). Inspect
the JSON response's `navigation_target` field.

If `navigation_target.kind === 'graph_node'`: follow the equivalent frontend path — either drive it through an
actual browser session or manually construct the URL `NAVIGATION_ROUTE_BUILDERS.graph_node` would produce
(`/blast-radius?system=...&node=...`) and load it — and confirm the Wave 1/2 investigation header names that
node, the node is visually marked `selected` (Wave 1's `graphColor.ts`), and (if it has real edges) the
Wave 2 investigation panel shows its relationships.

If `navigation_target` is `null` or has a different `kind` for this question: investigate why using the same
method 06.1-VERIFICATION.md used (inspect the evidence items in the response — is a `graph_relationship`
evidence item present at all? does `compute_navigation_target()`'s ambiguity logic see more than one candidate
destination?). Document the exact finding — do not guess.
</action>
<acceptance_criteria>
- A real `curl` transcript (request + response) is captured in this plan's execution notes showing the actual `navigation_target` value returned for a real blast-radius-style question
- If the happy path works: a description (or screenshot reference) confirming the redesigned Blast Radius page actually reflects the navigated-to node in its header and selection state
- If it does not work: a written root-cause finding (e.g. "no graph_relationship evidence item was surfaced because X") added to `backend/README.md` under a new `## Phase 06.1.1 Deviation` or `## Known Limitation` heading, following the project's existing deviation-documentation convention (see backend/README.md's existing Deviation N sections for format)
</acceptance_criteria>
</task>

<task id="2" type="execute">
<read_first>
- backend/README.md (existing Deviation N section format, to match exactly if a gap must be documented)
</read_first>
<action>
Based on task 1's finding, either: (a) if the happy path fully works, add a short confirmation note to `backend/README.md`'s existing Phase 06.1.1 area (or create one) stating D-09 is verified working end-to-end using Phase 06.1's existing infrastructure with no new backend code; or (b) if a genuine gap was found, document it precisely (what phrasing/data triggers it, why, and that it is out of this phase's frontend-only scope per D-12) so a future phase can pick it up deliberately rather than it being silently forgotten.
</action>
<acceptance_criteria>
- `backend/README.md` contains a clearly labeled section addressing this phase's D-09 verification outcome, one way or the other
- No new backend intent-classification code was added to close a gap found here (that would violate D-12's frontend-only scope) — `git diff --stat` shows zero changes under `backend/app/` beyond `backend/README.md` for this plan
</acceptance_criteria>
</task>
</tasks>

<verify>
1. `git diff --stat` for this plan's commit shows changes limited to `backend/README.md` and, if genuinely needed, `frontend/src/pages/BlastRadius.tsx` (documentation/minor wiring only — no new backend route/service file)
2. `backend/README.md` contains a real, specific finding (not a placeholder) for D-09
3. If a human/live session is unavailable to execute task 1's live `curl`/browser check, this plan's status must be reported as `human_needed`, not silently marked complete
</verify>
