---
phase: 06-product-experience
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - backend/app/routes/system_signals.py
  - backend/app/schemas.py
  - backend/app/main.py
  - backend/tests/test_routes_system_signals.py
  - frontend/src/pages/CommandCentre.tsx
  - frontend/src/components/ReadinessDial.tsx
  - frontend/src/components/HealthMiniCard.tsx
  - frontend/src/lib/api.ts
  - frontend/src/__tests__/CommandCentre.test.tsx
autonomous: true
requirements: [UI-03]

estimate:
  tokens: 46000
  raw_tokens: 46000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The Command Centre readiness dial is computed as passed_checks / total_checks from live GET /api/systems/{id}/assurance-cards responses across both seeded systems by default (D-06) — never from the stale gxp_systems.readiness_score column (61/94, never recomputed)."
    - "A system selector (All Systems / GXP-MFG-DEMO-01 / BUS-IT-DEMO-02) narrows the dial and all 6 mini-cards to one system (D-06, Discretion 4: dashboard-level control below the page heading)."
    - "Exactly 6 fixed mini-cards render in a fixed order — Documentation & Traceability, Periodic Review, Remediation & Approvals, Audit Trail Integrity, Access Reviews, Supplier Qualification — each traceable to exactly one deterministic backend source (D-07)."
    - "Mini-card #6 (Supplier Qualification) explicitly surfaces the overdue supplier by name (DataSync Solutions) when it is the overdue record, not just a count."
    - "A new GET /api/systems/{id}/access-supplier-signals endpoint is the only net-new backend route this phase's Command Centre needs — it follows the existing ungated-read-route precedent and the _system_exists guard pattern."
    - "Command Centre mini-card grid renders the empty-state heading 'No system data available' only if ALL backing calls fail; a single failed call renders that one card's own error state instead."
    - "Each of the 6 cards renders its own 'Loading…' placeholder independently — cards populate as their backing call resolves, not gated on all calls together."
    - "Command Centre renders the aggregate-fetch-failure copy as a dismissible banner on partial failure; already-loaded cards keep showing their data."
    - statement: "When one of the two SYSTEM_OPTIONS fetches fails and the other succeeds in 'All Systems' view, the dial and affected mini-cards compute from whichever system(s) responded, with a small 'partial data' note."
      verification: backstop
    - "Each mini-card's internal count reads as a bare 'N open'/'N pending' number — 0 is a normal (good) value, not an empty state."
    - "The system selector dropdown never overflows (3 fixed options), matching BlastRadius.tsx's existing <select> markup/classes verbatim."
    - "Mini-cards fade in with a subtle stagger (~40ms per card, opacity-0/translate-y-1 -> opacity-100/translate-y-0, <=300ms total) on mount, and are clickable/readable immediately regardless of the animation."
    - "The readiness dial's SVG arc animates stroke-dashoffset over 500ms only when its computed value actually changes, never on every re-render."
  artifacts:
    - backend/app/routes/system_signals.py
    - frontend/src/components/ReadinessDial.tsx
    - frontend/src/components/HealthMiniCard.tsx
    - frontend/src/__tests__/CommandCentre.test.tsx
    - backend/tests/test_routes_system_signals.py
  key_links:
    - "CommandCentre.tsx -> fetchAssuranceCards() x2 (existing) + fetchActionProposals() (existing) + fetchChainVerification() (new) + fetchSystemSignals() x2 (new) -> client-side aggregation (Promise.allSettled) -> dial % + 6 mini-cards"
    - "fetchSystemSignals() -> GET /api/systems/{id}/access-supplier-signals (new) -> suppliers/access_reviews tables, mirroring minimal_specialists.py's _check_a6 query pattern"
---

<objective>
Rebuild the Command Centre from a static placeholder into a real dashboard: a readiness dial and 6 health mini-cards computed live from the four existing backend surfaces (assurance-cards, actions, audit/verify) plus one genuinely new backend endpoint for overdue supplier/access signals — no stale seed columns, no fabricated domains.

Purpose: UI-03 — the Command Centre is the demo's landing page and the first proof-point that "deterministic, not an LLM opinion" is real, at a glance.
Output: frontend/src/pages/CommandCentre.tsx rebuilt; frontend/src/components/ReadinessDial.tsx and HealthMiniCard.tsx (new); backend/app/routes/system_signals.py (new); test coverage for both.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-product-experience/06-CONTEXT.md
@.planning/phases/06-product-experience/06-UI-SPEC.md
@.planning/phases/06-product-experience/06-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: New access/supplier-signals backend endpoint (D-07 mini-card #4/#5's data source)</name>
  <read_first>
    backend/app/routes/evidence_graph.py (`_system_exists` helper — import and reuse, do not redefine),
    backend/app/agents/minimal_specialists.py lines ~332-364 (`_check_a6` — the exact overdue-access-review and orphaned-privileged-account query pattern to mirror for the new suppliers query; this route needs only the overdue-count shape, not the orphan-account check),
    AegisX-AI-Project-Bible-v6.md lines 677-702 (`access_reviews`, `access_records`, `suppliers` DDL — column names: `suppliers(id, system_id, name, reassessment_due_date_ns, status)`, `access_reviews(id, system_id, review_type, scheduled_date_ns, status, ...)`),
    backend/app/schemas.py (existing response-model conventions to extend),
    backend/app/main.py (router-registration convention),
    backend/tests/test_routes_evidence_graph.py (UNIT/NEGATIVE/EDGE structure to mirror for a small read-only route)
  </read_first>
  <precondition>GXP-MFG-DEMO-01's seed data includes an overdue `suppliers` row (DataSync Solutions, Phase 1 ENV-03 seed, deliberately injected) and an overdue `access_reviews` row — the new endpoint has no real non-zero signal to prove correct without this seed data being present and unmodified.</precondition>
  <files>backend/app/routes/system_signals.py, backend/app/schemas.py, backend/app/main.py, backend/tests/test_routes_system_signals.py</files>
  <behavior>
    - GET /api/systems/GXP-MFG-DEMO-01/access-supplier-signals returns `overdue_suppliers >= 1` and `overdue_supplier_names` includes "DataSync Solutions".
    - GET /api/systems/{unknown-id}/access-supplier-signals returns 404.
    - An unreachable Postgres pool returns 503, not a fabricated zero-signal response.
  </behavior>
  <action>
    Create backend/app/routes/system_signals.py: `router = APIRouter()`. Add to backend/app/schemas.py: `class SystemSignalsResponse(BaseModel): system_id: str; overdue_access_reviews: int; overdue_suppliers: int; overdue_supplier_names: List[str]`. Handler `GET /api/systems/{system_id}/access-supplier-signals`: `pool = await acquire_pool_or_none()` -> 503 if None; `if not await _system_exists(pool, system_id): raise HTTPException(404, ...)` (import `_system_exists` from `app.routes.evidence_graph`, do not redefine it — a second copy is the same drift risk `opa_client.py`'s docstring warns against); `now_ns = time.time_ns()`; fetch overdue access reviews: `SELECT id FROM access_reviews WHERE system_id = $1 AND status != 'COMPLETED' AND scheduled_date_ns < $2` (mirrors `_check_a6` exactly, but as `pool.fetch` for a count, not `fetchrow` for a single record); fetch overdue suppliers: `SELECT id, name FROM suppliers WHERE system_id = $1 AND reassessment_due_date_ns < $2`; return `SystemSignalsResponse(system_id=system_id, overdue_access_reviews=len(review_rows), overdue_suppliers=len(supplier_rows), overdue_supplier_names=[r["name"] for r in supplier_rows])`. Every value bound through a `$N` placeholder, no f-string SQL (ASVS V5, matches every existing route in this codebase). Register in backend/app/main.py: import and `app.include_router(system_signals_router)`, append one paragraph to the module docstring following its existing "Plan 0X-XX registers a Nth router" convention. This route is deliberately left ungated (no RBAC, no identity dependency) — matching the explicit precedent `routes/actions.py`'s own docstring records for the Phase 4 read routes: read-only, non-PII aggregate GxP metadata, not a one-off exception to invent.
  </action>
  <verify>
    <automated>pytest tests/test_routes_system_signals.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `GET /api/systems/GXP-MFG-DEMO-01/access-supplier-signals` against live seeded Postgres returns `overdue_suppliers >= 1` with `"DataSync Solutions"` present in `overdue_supplier_names`.
    - `GET /api/systems/UNKNOWN-ID/access-supplier-signals` returns 404.
    - A test with the pool forced unreachable (mirrors `test_routes_evidence_graph.py`'s own unreachable-pool case) returns 503.
  </acceptance_criteria>
  <done>backend/tests/test_routes_system_signals.py passes against live seeded Postgres; no route elsewhere in the codebase duplicates this query.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Command Centre dashboard — dial + 6 mini-cards, client-side aggregation (D-06, D-07)</name>
  <read_first>
    frontend/src/pages/BlastRadius.tsx (the exact `<select>` markup/classes for the system selector to reuse verbatim: `rounded border border-slate-700 bg-slate-900 px-2 py-1 text-sm text-slate-100`),
    frontend/src/pages/Actions.tsx (the cancelled-guard `useEffect` fetch pattern),
    frontend/src/routes/findings.py's exported check-name grouping context: `backend/app/agents/a2_compliance.py` lines ~105-220 (`verify_urs_approved`, `verify_periodic_eval_current`, `verify_no_stale_documents`, `verify_test_traceability` — 4 checks total) and `backend/app/routes/actions.py`'s own docstring noting `verify_urs_approved` and `verify_no_stale_documents` both cite `ANNEX11-S4-DOC-001` (same rule family — both group into mini-card #1 alongside `verify_test_traceability`; `verify_periodic_eval_current` alone is mini-card #2),
    frontend/src/lib/api.ts (`fetchAssuranceCards`, `fetchActionProposals`, `AssuranceCardData.deterministic_check.check_name` — the exact field this task groups checks by),
    .planning/phases/06-product-experience/06-UI-SPEC.md "Interaction Notes" section (the full D-07 6-card mapping table and the exact readiness-dial computation paragraph — load-bearing, implement literally)
  </read_first>
  <files>frontend/src/components/ReadinessDial.tsx, frontend/src/components/HealthMiniCard.tsx, frontend/src/lib/api.ts, frontend/src/pages/CommandCentre.tsx, frontend/src/__tests__/CommandCentre.test.tsx</files>
  <behavior>
    - With both systems' assurance-cards returning 1 failing check each (4 checks x 2 systems = 8 total), the dial reads 75% and bands amber (50-79%).
    - Mini-card #1 counts failures among `{verify_urs_approved, verify_no_stale_documents, verify_test_traceability}`; mini-card #2 counts failures of `verify_periodic_eval_current` only — summed across the systems currently in scope.
    - Selecting a single system in the selector narrows the dial and all 6 cards to that system's data only.
  </behavior>
  <action>
    Create frontend/src/components/ReadinessDial.tsx: `export interface ReadinessDialProps { passed: number; total: number }`. Renders a fixed `160px x 160px` SVG arc (stroke-dasharray/stroke-dashoffset technique) with a caption showing `{Math.round((passed/total)*100)}%`. Color band per the UI-SPEC Color table: `>= 80` -> `emerald-600` stroke, `50-79` -> `amber-600`, `< 50` -> `orange-700`. Animate `stroke-dashoffset` via `transition: stroke-dashoffset 500ms ease-out`, guarding against animating on every re-render by comparing against the previous percent in a `useRef`/state pair — only transition when the computed percent actually changes.

    Create frontend/src/components/HealthMiniCard.tsx: `export interface HealthMiniCardProps { title: string; status: 'loading' | 'ready' | 'error'; children?: React.ReactNode }`. Renders the title as a Heading-styled label, "Loading…" body text while `status==='loading'`, an inline error line while `status==='error'`, else `children`. Mounts with `transition-all duration-300 ease-out` from `opacity-0 translate-y-1` to `opacity-100 translate-y-0`, taking an optional `style={{ transitionDelay }}` prop the caller staggers at ~40ms increments across the 6 cards (no stagger library).

    Extend frontend/src/lib/api.ts: add `export interface SystemSignalsResponse { system_id: string; overdue_access_reviews: number; overdue_suppliers: number; overdue_supplier_names: string[] }`, `export function fetchSystemSignals(systemId: string): Promise<SystemSignalsResponse>` (GET `/api/systems/${encodeURIComponent(systemId)}/access-supplier-signals`); and `export interface ChainVerificationResponse { status: string; events_checked: number | null; broken_at_index: number | null; event_id: string | null }`, `export function fetchChainVerification(): Promise<ChainVerificationResponse>` (GET `/api/audit/verify`) — mirroring `backend/app/schemas.py`'s `ChainVerificationResponse` field-for-field, matching this file's own established "mirrors backend/app/schemas.py... field for field" convention.

    Rewrite frontend/src/pages/CommandCentre.tsx. `SYSTEM_OPTIONS` constant identical to `BlastRadius.tsx`'s (`GXP-MFG-DEMO-01`, `BUS-IT-DEMO-02`). Selector state `scope: 'ALL' | 'GXP-MFG-DEMO-01' | 'BUS-IT-DEMO-02'` (default `'ALL'`), rendered as a `<select>` immediately below the page heading using BlastRadius.tsx's exact classes, options "All Systems" / the two system labels. Derive `systemsInScope: string[]` from `scope` (both ids, or the one selected).

    Fetch effect (cancelled-guard, re-runs on `scope` change): for each id in `systemsInScope`, call `Promise.allSettled([fetchAssuranceCards(id), fetchSystemSignals(id)])`; separately call `fetchActionProposals()` once (global, not per-system) and `fetchChainVerification()` once. Track per-call fulfilled/rejected outcomes rather than failing the whole page on one rejection (Promise.allSettled, not Promise.all) — a system whose calls fail contributes 0 to every count it would have fed and the page renders the aggregate-fetch-failure banner; if EVERY call across every system in scope fails, render the empty-state heading/body instead of the banner+cards.

    Compute: `totalChecks = 4 * fulfilledSystemsCount`; `failingCards = flatten(cards across fulfilled assurance-cards responses)`; `passed = totalChecks - failingCards.length`; feed `<ReadinessDial passed={passed} total={totalChecks} />` (guard `total === 0` -> render the empty state instead of a dial). Card 1 count = failing cards whose `deterministic_check.check_name` is in `{'verify_urs_approved','verify_no_stale_documents','verify_test_traceability'}`; Card 2 count = failing cards whose check_name is `'verify_periodic_eval_current'`; Card 3: from `fetchActionProposals()`'s `proposals`, filtered by `target_system` when `scope !== 'ALL'`, counted by `status` into PENDING_APPROVAL / (APPROVED or EXECUTED) / REJECTED; Card 4: `fetchChainVerification()`'s `status` (VERIFIED/TAMPERED); Card 5: sum of `overdue_access_reviews` across fulfilled `fetchSystemSignals` calls in scope; Card 6: sum of `overdue_suppliers` plus the deduplicated union of `overdue_supplier_names` (render the names list, explicitly surfacing "DataSync Solutions" when present — never just a bare count for this card).

    Render the 6 `HealthMiniCard`s in the fixed order from UI-SPEC's table: Documentation & Traceability, Periodic Review, Remediation & Approvals, Audit Trail Integrity, Access Reviews, Supplier Qualification — each with a ~40ms staggered `transitionDelay` (0ms, 40ms, 80ms, ... 200ms). Each card's own `status` is `'loading'` until ITS backing call(s) resolve, `'error'` if its own backing call(s) rejected while at least one other system's succeeded, independent of the other 5 cards. On a partial-systems failure in `'ALL'` scope (one of two systems' calls failed, the other succeeded), render a small inline "partial data" note near the dial/affected cards rather than the full-page error banner.
  </action>
  <verify>
    <automated>npm run test -- CommandCentre.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - With both systems' `fetchAssuranceCards` stubbed to return 1 failing card each (one `verify_periodic_eval_current`, one `verify_urs_approved`), the rendered dial shows `75%` (6/8, matching the <behavior> section's 4-checks-per-system-times-2-systems denominator) and cards 1 and 2 each show a count of `1`.
    - Selecting `GXP-MFG-DEMO-01` alone narrows the dial to that system's own 4-check denominator.
    - Stubbing one system's `fetchAssuranceCards` to reject while the other resolves renders the partial-data note, not the full empty-state.
    - Stubbing every call across both systems to reject renders the empty-state heading "No system data available".
    - Mini-card #6 renders "DataSync Solutions" by name when `fetchSystemSignals` returns it in `overdue_supplier_names`.
  </acceptance_criteria>
  <done>CommandCentre.test.tsx passes; the page never reads `gxp_systems.readiness_score`; the dial and all 6 cards compute from live backend data.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser -> GET /api/systems/{id}/access-supplier-signals | Unauthenticated read of aggregate, non-PII GxP metadata (supplier names, overdue counts) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-05 | Information Disclosure | GET /api/systems/{id}/access-supplier-signals | low | accept | Read-only, non-PII aggregate GxP metadata (supplier company names, overdue counts) — mirrors the existing ungated-read-route precedent (`evidence_graph.py`, `findings.py`, `audit.py`'s `GET /api/audit/verify`); no credential, session token, or per-user data is ever exposed by this endpoint. |
| T-06-06 | Denial of Service | GET /api/systems/{id}/access-supplier-signals, repeated calls, no rate limit | low | accept | Two lightweight indexed-column-comparison queries per call (`system_id` FK, a date comparison) — identical performance/threat profile to every other unauthenticated read route already shipped in Phases 4-5; no new exposure class introduced. |
| T-06-07 | Tampering | SQL injection via `system_id` path parameter | high | mitigate | Every query binds `system_id` through a `$N` asyncpg placeholder (never f-string interpolated), matching this codebase's established no-f-string-SQL discipline (`c1_verifier.py`, `evidence_graph.py`); `_system_exists()` (imported, not reimplemented) additionally 404s an unrecognized `system_id` before either query runs. |
</threat_model>

<verification>
- `pytest tests/test_routes_system_signals.py -x` and `npm run test -- CommandCentre.test.tsx` green.
- Full suites (`npm run test`, `pytest`) green before this plan is marked complete.
</verification>

<success_criteria>
The Command Centre shows a live readiness dial and 6 real health mini-cards, aggregated across both seeded systems by default and narrowable via a system selector, with no stale seed data and no fabricated domains.
</success_criteria>

<output>
Create `.planning/phases/06-product-experience/06-02-SUMMARY.md` when done
</output>
